# VIAJALY TRIP — Documento Único de Execução
> **Como usar**: coloque este arquivo na raiz do repositório `mzinhoww-svg/viaja-junto-comigo` (o mesmo do app de visto). Abra o Claude Code (Desktop, remoto ou terminal) nesse repositório e diga: **"Leia VIAJALY-TRIP.md e execute o protocolo."** Este arquivo é auto-suficiente: contém missão, regras, schema, backlog e log. Ele é reescrito por você mesmo (Claude Code) a cada ticket concluído — é sua própria memória entre sessões. Nunca crie CLAUDE.md, GUIA.md ou TICKETS.md separados: tudo vive aqui.
---
## 0. STATUS ATUAL — leia isto primeiro, sempre
| Campo | Valor |
|---|---|
| Onda atual | **Ondas 1 a 7 fechadas e o milestone v1 fechado no repositório.** Todos os tickets da Seção 7 estão mergeados: VJT-001 → VJT-017b, mais VJT-006b (só no Log), **VJT-018** (QA mobile, PR [#81](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/81), mergeado 2026-07-26) e **VJT-019** (auditoria final, PR [#79](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/79), mergeado 2026-07-26). **Não há ticket de feature pendente.** O que separa o v1 de "lançado" não é mais código: são as pendências de painel listadas abaixo |
| Último ticket concluído (mergeado) | VJT-018 (PR [#81](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/81)) e VJT-019 (PR [#79](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/79)), ambos em 2026-07-26. Último merge de qualquer natureza: PR [#84](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/84) (exclusão de conta via `createServerFn`). CI da `main`: **verde** |
| Ticket em aberto aguardando review | **Nenhum.** Os 4 PRs que estavam parados (#71, #77, #79, #81) foram resolvidos em 2026-07-26 — ver a linha "Autorização excepcional" abaixo |
| 🚨 O que falta para o v1 estar de fato no ar | **Nada de código. Cinco ações de painel, todas suas** — em ordem de dano: **(1)** publicar a `main` (a exclusão de conta só passa a funcionar depois disso); **(2)** `VITE_SENTRY_DSN`; **(3)** virar o Stripe de `pk_test_` para `pk_live_` (+ secret live + `PAYMENTS_ENABLED=true`); **(4)** `VITE_POSTHOG_KEY` + criar os 2 funis no painel do PostHog; **(5)** `TRIP_ADMIN_CODE` + `TRIP_ADMIN_EMAIL`. Detalhe e dano de cada uma em `reports/auditoria-estado-2026-07-26.md` |
| Publicação (estado em 2026-07-26) | **A `main` FOI publicada em 2026-07-26** e o atraso de ~19h que o VJT-019 reportou está **resolvido**: `/trip/login` responde **307 → 200** (era 404), e os marcadores de `Acesso da equipe` (VJT-011d), `Continuar com Google` (`69a004e`) e `destino_pais, destino_cidade, num_criancas` (VJT-011b) aparecem no bundle publicado — varredura completa de **101 chunks**. O item "mergeado mas não publicado" do VJT-019 está **encerrado**. ⚠️ **Mas a publicação não é automática** (ver Seção 3): os merges de 2026-07-26 (#83, #81, #77, #79, #84) **ainda não estão no ar** — o build publicado é a `main` em `8ed7fc7`, confirmado porque o polimento do VJT-018 não aparece (o bundle ainda traz `Close`, não `Fechar`) |
| 🚨 Entregue mas **inerte** em produção (3 itens, todos por pendência de painel) | **(1) PostHog / VJT-015** — `VITE_POSTHOG_KEY` **não existe** no build. O `import.meta.env` inlinado tem exatamente 4 variáveis: `VITE_PAYMENTS_CLIENT_TOKEN`, `VITE_SUPABASE_PROJECT_ID`, `VITE_SUPABASE_PUBLISHABLE_KEY`, `VITE_SUPABASE_URL`. **Zero eventos** desde o merge; os 12 nomes de evento estão corretos no bundle. Os 2 funis também precisam ser criados à mão. ⚠️ Armadilha de leitura: os nomes `VITE_POSTHOG_KEY`/`VITE_POSTHOG_HOST`/`VITE_ANALYTICS_ENABLED` **aparecem** como texto nos chunks, mas só como leitura de propriedade no gate (`e.VITE_POSTHOG_KEY?.trim()`) — contar ocorrências dá falso positivo; o que vale é a chave existir no objeto env. **(2) Sentry / VJT-001** — `VITE_SENTRY_DSN` ausente: **0 ocorrências da string `sentry`** em nenhum chunk, o SDK inteiro sai do bundle. Erro de produção não é reportado a lugar nenhum. **(3) Stripe / VJT-012** — o build publicado usa **`pk_test_`**: o checkout roda em sandbox e **não cobra ninguém**. O webhook `/api/public/payments/webhook` está publicado e responde 200. `PAYMENTS_ENABLED` é *fail-closed* e, por ser env de servidor, não é auditável de fora |
| ✅ Exclusão de conta (LGPD, VJT-017) — **corrigida em código, aguardando publicação** | Era o 4º item inerte. A Edge Function `delete-account` **nunca foi deployada** (`GET /functions/v1/delete-account` → 404, com `lock-usd-rate`/`refresh-usd-reference` respondendo no mesmo projeto e controle negativo calibrado): o botão estava publicado chamando um endpoint inexistente desde o VJT-017. **Diagnóstico corrigido**: não era o bundler (a function não importava nada de `src/`) — **não existe pipeline que deploye Edge Functions neste repositório**. Migrada para `createServerFn` no PR [#84](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/84), mesmo caminho da `ai-chat`, e o `supabase/functions/delete-account` foi removido. **Só passa a funcionar quando a `main` for publicada** |
| ⚠️ Envs do código de acesso (VJT-011c/VJT-011d) | **`TRIP_ADMIN_CODE` e `TRIP_ADMIN_EMAIL` ainda não cadastradas.** Sem elas o Caminho A do runbook fica **desligado** — o card em `/trip/mais` e o "Acesso da equipe" em `/trip/login` respondem que o recurso está desativado neste ambiente. A ativação fora do checkout depende inteiramente do Caminho B (SQL Editor) |
| Migrations aplicadas no Supabase | **Nenhuma pendente.** Verificado em 2026-07-26 por consulta direta ao catálogo do Postgres (`information_schema`, `pg_class`, `pg_policies`, `pg_proc`, `pg_constraint`, `pg_indexes`, `pg_publication_tables`) — **as 19 tabelas existem e as 19 estão com RLS ON**, `checklist_templates` = **142**, `paises_visto` = **14**, `trips.num_criancas`, `idx_entitlements_stripe_payment_id`, `entitlements` na publication `supabase_realtime`, e as funções `is_trip_member`, `trip_owner_id`, `accept_trip_invite`, `increment_ai_usage`, `ai_daily_message_count`. A `20260725220000_vjt017b_lgpd_consent_history.sql`, que a Seção 0 já chamou de "única pendente", **está aplicada** (PK = `id`, unique `user_lgpd_consents_user_versao_unique`). FKs de exclusão conferidas uma a uma: `savings_entries.created_by` = **SET NULL**, todas as demais = **CASCADE** |
| Acesso ao banco pelo conector | **Existe, pelo conector Lovable** — corrige um registro errado que sobreviveu em três lugares deste arquivo. O `project_id` (`a357c84b-f9f7-4cb0-8d4e-a9798a04ecbd`) está publicado no HTML de produção, no badge do rodapé. O que **não** alcança o projeto é o conector **Supabase** (só enxerga `crm-ai-studio`, `supabase-purple-book`, `the-loyalty`). Consequência: verificação de banco **não** exige mais rodar SQL à mão no painel |
| Dados em produção | **Base vazia**: 0 viagens, 0 entitlements, 0 consentimentos LGPD, 0 mensagens de IA, 0 respostas de NPS (medido em 2026-07-26). Nenhum usuário real ainda — é por isso que quase toda pendência acima custa quase nada agora e fica cara depois |
| Consentimento LGPD / subprocessadores (VJT-017b) | **Resolvido e no ar.** O bundle publicado nomeia os 4 subprocessadores com país e papel (Supabase/EUA, Stripe/EUA, OpenRouter/EUA roteamento, DeepSeek/China inferência) e descreve a transferência internacional. Migration aplicada. **A pendência "bloqueia o deploy da Edge Function `ai-chat`" está obsoleta**: não existe mais Edge Function de IA — `e268aca` migrou o assistente para `createServerFn`, que sobe junto com o app |
| Variáveis só de servidor (não auditáveis de fora) | `PAYMENTS_ENABLED`, `STRIPE_SANDBOX_API_KEY`/`STRIPE_LIVE_API_KEY`, `OPENROUTER_API_KEY`, `AI_MODEL`, `AI_ENABLED`, `AI_DAILY_MSG_CAP`, `TRIP_ADMIN_CODE`, `TRIP_ADMIN_EMAIL`, `SUPABASE_SERVICE_ROLE_KEY`. Nenhuma aparece no client — varredura dos chunks publicados: **0 ocorrências** de `service_role`, `sk_`, `whsec_`, `OPENROUTER`, `TRIP_ADMIN`; o único JWT publicado tem `"role":"anon"`. Conferência manual **no painel do Lovable** (ver Seção 3 — não é Vercel). Defaults: `AI_ENABLED` ausente = **ligado**; `VITE_INVITES_ENABLED` ausente = **ligado**; `PAYMENTS_ENABLED` ausente = **desligado** (fail-closed) |
| Autorização excepcional de merge (2026-07-26) | **O dono do produto autorizou, nesta data, mergear sem revisão humana prévia** os PRs #77, #79, #81, #83 e o da correção da exclusão de conta (#84), desde que com CI verde no head commit — exceção explícita e datada ao passo 12 da Seção 6. Justificativa registrada por ele: base de usuários zero, PRs majoritariamente markdown, e necessidade de fechar o milestone. **Não é precedente**: a regra volta a valer integralmente, e todo merge futuro é humano |
| PRs/issues abertos | **Nenhum PR aberto.** PR #71 foi **fechado sem merge** por obsolescência (o conserto já estava na `main` via `3ec1e51`/PR #72, verificado no arquivo; e os 2190 erros de lint que ele reportava foram resolvidos pelo PR #74). Issues #76, #78 e #80 fecham com os merges de #77, #79 e #81 |
| Branch `claude/vjt-003-onboarding-wizard` (do PR #18 descartado) | **Ainda não apagada**: `git push origin --delete` retorna 403 (política do proxy de git, não credencial). PR e issue já fechados; apagar à mão no GitHub (Settings → Branches). Na mesma varredura: **37 das 38 branches do repo são órfãs** de tickets já mergeados |
| Revisor automatizado (`.github/workflows/revisor-automatizado.yml`, Seção 6) | **Desativado** (gatilho em `workflow_dispatch`), baixa prioridade — diagnóstico completo na entrada de 2026-07-25 da Seção 8. CI normal (lint/typecheck/test/build) não é afetado. Retomar só com autorização explícita do humano |
| Última atualização desta seção | **2026-07-26** — sessão de fechamento do milestone. Fase 1: mergeados #83, #81 (VJT-018), #77 e #79 (VJT-019); #71 fechado sem merge com a recomendação registrada no PR. Fase 2: exclusão de conta corrigida (#84). Fase 3: esta seção reescrita inteira contra estado verificado em três camadas, e **a premissa "Vercel" corrigida em todo o documento** (ver Seção 3). Divergências corrigidas nesta edição: (a) a hospedagem não é Vercel; (b) a publicação não é automática; (c) VJT-018 e VJT-019 foram executados e mergeados — a linha "VJT-018 nunca foi executado" estava correta quando escrita e ficou obsoleta em horas; (d) o banco É acessível por conector; (e) não há migration pendente; (f) o `delete-account` deixou de ser Edge Function |
**Protocolo de toda sessão, sem exceção:**
1. Leia esta seção 0 e a seção 8 (Log). Se este arquivo e o estado real do repositório divergirem (ex.: PR já mergeado mas não registrado aqui), reconcilie a seção 0 e o log ANTES de fazer qualquer coisa nova
2. Determine o próximo ticket executável: primeiro ticket da seção 7 cujo "Bloqueado por" já está com status Mergeado no log, e que ainda não tem entrada de conclusão
3. Se não houver ticket executável (tudo bloqueado ou tudo feito), reporte isso e pare
4. Execute o ticket seguindo a seção 6 (protocolo de execução)
5. Ao final: atualize a seção 0 (estado) e adicione uma entrada na seção 8 (log), na mesma edição deste arquivo, e pare — aguarde review e merge humano antes de seguir para o próximo
6. Nunca faça merge de PR. Essa ação é sempre humana
---
## 1. Missão e contexto
Viajaly Trip é o companion de planejamento de viagens internacionais da Viajaly. Entra como rotas `/trip/*` dentro do app existente (mesmo repositório, mesmo Supabase, mesmo Stripe, mesmo deploy — que é **Lovable**, ver Seção 3) — não é produto de repositório separado.
**Tese**: transformar uma viagem cara e complexa em um plano mensal visível e compartilhado. Grátis adquire audiência; premium monetiza; destinos que exigem visto geram leads contextuais (nunca bloqueantes) para a consultoria Viajaly.
**Personas**: P1 planejadora de família (prioridade máxima — toda decisão de UX resolve ela primeiro); P2 casal na primeira viagem internacional (ativa a colaboração); P3 viajante recorrente solo (beneficiária passiva do multi-trip premium).
**Referência funcional**: mecânica do "Disney Smart by Magic Dani" (dashboard de progresso, countdown, checklists, financeiro com meta mensal, roteiro com PDF), generalizada para qualquer destino, com três diferenciais: moeda dual com câmbio manual, colaboração entre viajantes, assistente IA contextual.
---
## 2. Regras não negociáveis
**Fórmulas** — implementar em módulo puro `trip-math` (nome de arquivo à escolha do agente na Fase 1), com testes unitários:
```
meta                  = Σ valor_estimado consolidado em BRL de todos os budget_items
consolidado_brl       = valor_brl ?? (valor_destino × cambio_manual)
acumulado             = Σ savings_entries + Σ valor_pago consolidado
progresso_financeiro  = acumulado / meta                         (meta 0 → indicador oculto + CTA)
meses_restantes       = max(1, floor(meses até data_viagem))
sugestao_mensal       = max(0, (meta − acumulado) / meses_restantes)
progresso_checklists  = itens done / itens totais                (global da trip)
progresso_jornada     = 0.5 × progresso_checklists + 0.5 × progresso_financeiro   (pesos em constante, não hardcoded)
```
**Distinção de dependência**: tickets que consomem valores agregados de trip-math (razões, percentuais, modo sonho/planejando/concluída) não dependem da completude do catálogo de checklists — a fórmula é agnóstica ao volume de itens. Só tickets que renderizam o CONTEÚDO individual dos itens (ex.: UI de checklists, packs por destino) dependem de catálogo completo. Ao definir "Bloqueado por" de um novo ticket, aplique essa distinção antes de adicionar dependência por precaução.
**Edge cases obrigatórios em toda entrega**: sem data de viagem (modo sonho — sem countdown, sem sugestão mensal); data no passado (modo concluída — retrospectiva + NPS + CTA nova viagem); meta zero; valor pago > estimado na categoria (badge de estouro, nunca bloqueia); divisão por zero em qualquer fórmula acima.
**Planos e paywall**:
| Recurso | Free | Premium |
|---|---|---|
| Viagens | 1 | Ilimitadas |
| Membros por viagem | Solo | Até 5 |
| Checklists | Básicos (~30 itens) | Completos (80-120) + packs por destino |
| Financeiro (economia + orçamento) | Completo | Completo |
| Roteiro | Até 5 dias | Ilimitado |
| Export PDF | Não | Sim |
| Assistente IA | 10 msgs/mês | 100 msgs/mês |
- Premium é **por usuário** (não por viagem), one-time **R$ 97** (lançamento R$ 67), ou bônus incluso apenas nos pacotes **Pro+ e Vip+** da consultoria (Start+ fica de fora — vira argumento de upsell interno)
- Fonte única de plano: tabela `entitlements`. Um único hook/helper no client (`useEntitlement()` ou equivalente). Nunca checar plano ad hoc em componente
- 6 gatilhos de paywall, todos abrindo o MESMO modal: criar 2ª viagem, convidar membro, adicionar 6º dia de roteiro, exportar PDF, esgotar cota de IA, abrir item de checklist premium
- Item "Visto" no checklist Documentos aparece só quando o destino exige visto para brasileiros (tabela `paises_visto`); card com link com UTM para a consultoria; **nunca bloqueante**
**Segurança**: RLS em TODAS as tabelas do produto via função `is_trip_member(trip_id)` (security definer). Nunca desabilitar RLS. Nunca usar service role no client. Convites via token opaco com expiração de 7 dias; aceite via RPC própria (o convidado ainda não é membro, então não passa nas policies normais).
**Fora de escopo (não implementar sem pedido explícito)**: parcelamento no orçamento, notificações push/email, edição colaborativa em tempo real, API de câmbio automático, split de custos entre membros, IA escrevendo direto no roteiro, app nativo.
**Regras de fatiamento de ticket**: nunca existe ticket separado só de testes (testes vivem no ticket da feature); migration junto da feature que a usa; um PR não trivial mira menos de ~400 linhas; ticket grande demais é quebrado antes de começar; feature arriscada nasce com kill switch (flag de ambiente) e observabilidade.
---
## 3. Stack e conectores já disponíveis
- Frontend: React + TypeScript + Vite/Next (o que a codebase existente já usa) + Tailwind, adicionado como rotas `/trip/*` dentro do repositório atual
- Backend: Supabase (Postgres, Auth, RLS, Edge Functions) — **mesmo projeto** do app de visto, login automaticamente compartilhado
- Pagamentos: Stripe, checkout one-time — **mesma conta** já integrada ao app
- IA: **OpenRouter**, chamado por server function do TanStack Start (`src/lib/ai-chat.functions.ts`), chave nunca no client. Modelo econômico — hoje `deepseek/deepseek-chat`, escolhido por custo bem abaixo da classe Haiku. Decisão do dono do produto em 2026-07-25, substituindo a Anthropic direta da versão original desta seção. O identificador do modelo é sobrescrevível pelo secret `AI_MODEL` sem novo deploy. **Deixou de ser Edge Function** no commit `e268aca`: o bundler do Cloud não empacota arquivos fora de `supabase/functions/`, e a função importava `src/lib/*` para não duplicar `entitlements`/`trip-math`/`trip-visa`
- **Deploy: Lovable, servido por Cloudflare — NÃO é Vercel, e NÃO publica sozinho.** Ver a nota de hospedagem abaixo: é a correção mais importante desta seção
- Observabilidade: Sentry (erros) + PostHog (produto), a configurar nos tickets VJT-001 e VJT-016

> ### 🚨 Hospedagem: é Lovable, não Vercel (corrigido em 2026-07-26)
>
> Este documento afirmou por 12 dias que o deploy era **Vercel** e que "todo PR mergeado na main publica sozinho". **As duas metades estavam erradas**, e o custo foi concreto: variáveis de ambiente foram procuradas no painel errado, e um atraso de ~19h entre merge e publicação foi lido como falha de esteira da Vercel — que não existe neste projeto.
>
> Evidência medida (auditoria de 2026-07-26, `reports/auditoria-estado-2026-07-26.md`):
>
> | O quê | Resultado |
> |---|---|
> | DNS de `viajaly.com` | `185.158.133.1` (faixa da Lovable; Vercel seria `76.76.21.x`) |
> | Cabeçalhos de resposta | `server: cloudflare`, `x-deployment-id: …`. **Zero** cabeçalhos `x-vercel-*` |
> | API da Lovable | `is_published: true`, `url: https://viaja-junto-comigo.lovable.app` |
> | `.lovable.app` vs `viajaly.com` | **mesmo `x-deployment-id`** e mesmo chunk de entrada (o `.lovable.app` só faz 302 para o domínio próprio) |
>
> **Consequências operacionais, que valem para toda sessão futura:**
> 1. **Toda variável de ambiente se cadastra no painel do Lovable** (`VITE_SENTRY_DSN`, `VITE_POSTHOG_KEY`, `TRIP_ADMIN_CODE`, `TRIP_ADMIN_EMAIL`, secrets do Stripe e do OpenRouter). Não há projeto na Vercel onde procurar.
> 2. **Publicar é um ato manual.** Merge na `main` não publica. Um agente **nunca** deve afirmar que algo "está no ar" por ter sido mergeado — só por medição contra o site.
> 3. **Não existe pipeline que deploye Edge Functions.** Nenhum workflow em `.github/workflows/` menciona Supabase. Foi por isso que a `delete-account` ficou 404 desde o VJT-017 sem ninguém notar. **Regra**: funcionalidade nova de servidor nasce como `createServerFn`, que sobe junto com o app — não como Edge Function.
> 4. **Entradas antigas da Seção 8 (Log) ainda dizem "Vercel".** Não foram reescritas de propósito: o Log é registro histórico do que se acreditava na época. **Leia toda menção a "painel da Vercel" no Log como "painel do Lovable".**

**Conectores ativos nesta conta**: GitHub (repo `mzinhoww-svg/viaja-junto-comigo` — issues, PRs, CI, commits), **Lovable (alcança o banco de produção e executa SQL — ver nota abaixo)**, Supabase (não alcança este projeto), Vercel (só web analytics, e sem relação com o deploy deste app), Claude Code Remote (ambientes, sessões, rotinas agendadas). **Não há Linear nem Orca conectados** — por isso: tickets vivem como Issues do GitHub (não Linear), e paralelismo vem de sessões/ambientes do Claude Code Remote (não Orca/Conductor). Use os conectores diretamente; nunca peça ao usuário um token para o que a conexão já cobre.
**Acesso ao banco (corrigido em 2026-07-26)**: o conector **Supabase** desta conta só enxerga `crm-ai-studio`, `supabase-purple-book` e `the-loyalty` — não alcança `urrlqljlibpzaqnemlwf`. Isso levou o documento a registrar, em três lugares, que o banco era "não verificável" e que migrations exigiam SQL manual no painel. **É falso**: o conector **Lovable** alcança o mesmo banco e executa SQL, usando o `project_id` `a357c84b-f9f7-4cb0-8d4e-a9798a04ecbd`, que está publicado no HTML de produção (badge do rodapé). Verificação de banco não exige mais ação manual — o que exige é aplicar migration nova, que segue sendo passo humano.
**Limitação conhecida**: exclusão de branch remota (`git push --delete`) retorna 403 nesta sessão — política do proxy de git, não é credencial. Quando um PR for fechado sem merge, deixe a branch marcada para exclusão manual pelo humano (GitHub → Branches) em vez de tentar apagar via git.
**Decisão técnica vigente (VJT-009, 2026-07-25)**: fallback WhatsApp do item de visto é o caminho único em produção até `link_consultoria` ser populado manualmente na tabela `paises_visto` — decisão aceita conscientemente, não pendência não-resolvida.
**Regra de processo (VJT-013, 2026-07-25)**: todo ticket que toca RLS/policies deve rodar `scripts/test-rls.sh --audit` e reportar o resultado no PR, não apenas afirmar "nenhuma policy nova" ou "reaproveita policy existente" — essas afirmações são de procedência, não de execução, e não substituem teste real (achado do VJT-013, PR [#59](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/59)).

### Desvios de processo registrados (auditoria de 2026-07-26)

**(a) O VJT-006b foi implementado e mergeado DUAS vezes.** Issues [#36](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/36) e [#37](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/37), PRs [#38](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/38) e [#40](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/40) — mergeados com **62 segundos de diferença** (00:12:35Z e 00:13:37Z). Não é que a checagem de duplicata do passo 2 da Seção 6 tenha sido pulada: as duas sessões a executaram **simultaneamente**, e nesse instante nenhuma das duas issues existia ainda para a outra encontrar. A checagem é uma leitura sem trava — ela não protege contra concorrência, só contra trabalho antigo. O trabalho duplicado **entrou na `main`**; nenhum CI barra isso, porque os dois PRs eram individualmente válidos.

> **Mitigação proposta** (barata e suficiente para um time deste tamanho, em ordem de esforço):
> 1. **Reservar a issue ANTES de escrever código.** Abrir a issue é o primeiro ato do ticket, não um passo intermediário: a issue publicada é o cadeado. Se ao abrir você descobre que já existe, perdeu 30 segundos, não uma sessão.
> 2. **Reler a busca de duplicata imediatamente antes de abrir o PR**, não só no início. A janela perigosa é justamente a duração da execução — o VJT-011b caiu nela também (o PR #72 mergeou 41s depois do #75 e tornou obsoleto um runbook recém-escrito).
> 3. **Declarar no corpo da issue os arquivos que o ticket vai tocar.** Torna a colisão visível numa busca por caminho, não só por código do ticket.
> 4. Se um dia o paralelismo crescer: rótulo `em-execucao` aplicado na issue no momento da criação da branch, e a busca de duplicata passa a filtrar por ele.

**(b) O editor Lovable empurra commits direto para a `main`, sem PR e sem CI.** Foram **8 commits** assim (`Changes`, `Vendorizou libs para deploy`, `Fixed AB types & console routes`, `Migrou AI para createServerFn`, `Adicionou botão Google`, entre outros). Dois danos medidos: (i) deixaram **2190 erros de `prettier/prettier`** em 15 arquivos, o que fez **todo PR novo nascer com o lint vermelho** até o PR [#74](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/74) consertar; (ii) **o build que esteve no ar até 2026-07-26 veio de um desses commits, com CI vermelho** (`e268aca`/`8a6438c`). Também são a origem do estrago de 2026-07-25 na `main`.

> **Três opções, e a recomendação:**
>
> | Opção | O que dá | O que custa |
> |---|---|---|
> | **1. Aceitar como está** | Zero atrito no editor visual — o dono do produto continua editando e publicando sozinho | A `main` continua podendo quebrar sem aviso, e o próximo PR herda o estrago. Já aconteceu duas vezes |
> | **2. Job de auto-formatação na `main`** | Um workflow em `push: main` roda `prettier --write` e commita de volta. Mata o dano (i) por completo, sem tirar nada de ninguém | Não resolve o dano (ii): commit quebrado continua entrando e podendo ser publicado |
> | **3. Branch protection exigindo PR + CI verde** | Mata (i) e (ii) | **Quebra o fluxo do editor Lovable**, que empurra direto. Na prática inviabiliza o uso do editor visual — que é como o dono do produto trabalha |
>
> **Recomendação: opção 2 agora, opção 3 só se um segundo desenvolvedor entrar no repositório.** A 3 é tecnicamente superior e operacionalmente errada para este projeto hoje: tirar o editor visual das mãos do dono do produto custa mais do que os dois incidentes custaram. A 2 é um arquivo de workflow, elimina a classe de problema que de fato atrapalhou o dia a dia (lint vermelho contaminando todo PR), e não pede que ninguém mude como trabalha. O risco residual de (ii) fica coberto por uma disciplina barata, já que publicar é manual de qualquer jeito: **conferir o CI do commit antes de clicar em publicar**.
Valores monetários: sempre em centavos (integer). UI 100% em PT-BR; código e commits em inglês, padrão de commits convencional (feat/fix/chore). Mobile-first (base 375px), bottom nav de 5 itens (Jornada, Financeiro, Checklists, Roteiro, Mais). Todo card tem empty state com CTA — tela vazia é bug.
---
## 4. Modelo de dados (aplicar via Supabase na Fase 1 / VJT-001)
```sql
-- =============================================================
-- Viajaly Trip — Schema inicial (MVP)
-- Valores monetários em CENTAVOS (integer).
-- RLS por pertencimento à trip via is_trip_member().
-- =============================================================
create type trip_status as enum ('sonho', 'planejando', 'concluida');
create type member_role as enum ('owner', 'editor');
create type checklist_type as enum ('documentos', 'preparativos', 'mala', 'compras', 'custom');
create type slot_period as enum ('manha', 'tarde', 'noite');
create type plan_tier as enum ('free', 'premium');
create type entitlement_origin as enum ('stripe', 'pacote_visto', 'manual');
create table public.trips (
  id uuid primary key default gen_random_uuid(),
  owner_id uuid not null references auth.users(id) on delete cascade,
  nome text not null default 'Minha Viagem',
  destino_pais text not null,
  destino_cidade text,
  data_viagem date,
  num_pessoas int not null default 1 check (num_pessoas between 1 and 10),
  moeda_destino char(3),
  cambio_manual numeric(12,4),
  cambio_atualizado_em timestamptz,
  status trip_status not null default 'planejando',
  created_at timestamptz not null default now()
);
create table public.trip_members (
  trip_id uuid not null references public.trips(id) on delete cascade,
  user_id uuid not null references auth.users(id) on delete cascade,
  role member_role not null default 'editor',
  joined_at timestamptz not null default now(),
  primary key (trip_id, user_id)
);
create table public.trip_invites (
  id uuid primary key default gen_random_uuid(),
  trip_id uuid not null references public.trips(id) on delete cascade,
  token text not null unique default encode(gen_random_bytes(24), 'hex'),
  created_by uuid not null references auth.users(id),
  accepted_by uuid references auth.users(id),
  expires_at timestamptz not null default now() + interval '7 days',
  created_at timestamptz not null default now()
);
create table public.budget_categories (
  id uuid primary key default gen_random_uuid(),
  trip_id uuid not null references public.trips(id) on delete cascade,
  nome text not null,
  cor text not null default '#0EA5E9',
  ordem int not null default 0,
  is_default boolean not null default false
);
create table public.budget_items (
  id uuid primary key default gen_random_uuid(),
  trip_id uuid not null references public.trips(id) on delete cascade,
  category_id uuid not null references public.budget_categories(id) on delete cascade,
  nome text not null,
  valor_estimado_brl_cents bigint check (valor_estimado_brl_cents >= 0),
  valor_estimado_destino_cents bigint check (valor_estimado_destino_cents >= 0),
  valor_pago_brl_cents bigint not null default 0 check (valor_pago_brl_cents >= 0),
  valor_pago_destino_cents bigint not null default 0 check (valor_pago_destino_cents >= 0),
  nota text,
  created_at timestamptz not null default now(),
  check (valor_estimado_brl_cents is not null or valor_estimado_destino_cents is not null)
);
create table public.savings_entries (
  id uuid primary key default gen_random_uuid(),
  trip_id uuid not null references public.trips(id) on delete cascade,
  mes_ano date not null,
  valor_brl_cents bigint not null check (valor_brl_cents > 0),
  created_by uuid not null references auth.users(id),
  created_at timestamptz not null default now(),
  unique (trip_id, mes_ano, created_by)
);
create table public.checklists (
  id uuid primary key default gen_random_uuid(),
  trip_id uuid not null references public.trips(id) on delete cascade,
  tipo checklist_type not null,
  nome text not null,
  ordem int not null default 0
);
create table public.checklist_items (
  id uuid primary key default gen_random_uuid(),
  checklist_id uuid not null references public.checklists(id) on delete cascade,
  titulo text not null,
  done boolean not null default false,
  nota text,
  prazo_dias_antes int,
  marco int,
  ordem int not null default 0
);
create table public.checklist_templates (
  id uuid primary key default gen_random_uuid(),
  tipo checklist_type not null,
  titulo text not null,
  marco int,
  prazo_dias_antes int,
  tier plan_tier not null default 'free',
  regiao text,
  clima text,
  min_duracao int,
  com_crianca boolean,
  destino_pack text,
  ordem int not null default 0
);
create table public.itinerary_days (
  id uuid primary key default gen_random_uuid(),
  trip_id uuid not null references public.trips(id) on delete cascade,
  dia_numero int not null check (dia_numero >= 1),
  data date,
  ordem int not null default 0,
  unique (trip_id, dia_numero)
);
create table public.itinerary_slots (
  id uuid primary key default gen_random_uuid(),
  day_id uuid not null references public.itinerary_days(id) on delete cascade,
  periodo slot_period not null,
  onde_ir text,
  onde_comer text,
  observacoes text,
  unique (day_id, periodo)
);
create table public.ai_conversations (
  id uuid primary key default gen_random_uuid(),
  trip_id uuid not null references public.trips(id) on delete cascade,
  created_by uuid not null references auth.users(id),
  created_at timestamptz not null default now()
);
create table public.ai_messages (
  id uuid primary key default gen_random_uuid(),
  conversation_id uuid not null references public.ai_conversations(id) on delete cascade,
  role text not null check (role in ('user', 'assistant')),
  content text not null,
  created_at timestamptz not null default now()
);
create table public.ai_usage (
  user_id uuid not null references auth.users(id) on delete cascade,
  mes_ano date not null,
  msgs_count int not null default 0,
  primary key (user_id, mes_ano)
);
create table public.entitlements (
  user_id uuid primary key references auth.users(id) on delete cascade,
  plano plan_tier not null default 'free',
  origem entitlement_origin,
  stripe_payment_id text,
  expires_at timestamptz,
  created_at timestamptz not null default now(),
  updated_at timestamptz not null default now()
);
create table public.paises_visto (
  pais_iso char(2) primary key,
  pais_nome text not null,
  exige_visto_br boolean not null,
  tipo_visto text,
  link_consultoria text
);
create index idx_trip_members_user on public.trip_members(user_id);
create index idx_budget_items_trip on public.budget_items(trip_id);
create index idx_budget_items_category on public.budget_items(category_id);
create index idx_savings_trip on public.savings_entries(trip_id);
create index idx_checklists_trip on public.checklists(trip_id);
create index idx_checklist_items_list on public.checklist_items(checklist_id);
create index idx_itinerary_days_trip on public.itinerary_days(trip_id);
create index idx_itinerary_slots_day on public.itinerary_slots(day_id);
create index idx_ai_conversations_trip on public.ai_conversations(trip_id);
create or replace function public.is_trip_member(p_trip_id uuid)
returns boolean language sql security definer set search_path = public stable as $$
  select exists (select 1 from public.trip_members where trip_id = p_trip_id and user_id = auth.uid());
$$;
create or replace function public.is_trip_owner(p_trip_id uuid)
returns boolean language sql security definer set search_path = public stable as $$
  select exists (select 1 from public.trip_members where trip_id = p_trip_id and user_id = auth.uid() and role = 'owner');
$$;
alter table public.trips enable row level security;
alter table public.trip_members enable row level security;
alter table public.trip_invites enable row level security;
alter table public.budget_categories enable row level security;
alter table public.budget_items enable row level security;
alter table public.savings_entries enable row level security;
alter table public.checklists enable row level security;
alter table public.checklist_items enable row level security;
alter table public.checklist_templates enable row level security;
alter table public.itinerary_days enable row level security;
alter table public.itinerary_slots enable row level security;
alter table public.ai_conversations enable row level security;
alter table public.ai_messages enable row level security;
alter table public.ai_usage enable row level security;
alter table public.entitlements enable row level security;
alter table public.paises_visto enable row level security;
create policy trips_select on public.trips for select using (is_trip_member(id));
create policy trips_insert on public.trips for insert with check (owner_id = auth.uid());
create policy trips_update on public.trips for update using (is_trip_member(id));
create policy trips_delete on public.trips for delete using (is_trip_owner(id));
create policy members_select on public.trip_members for select using (is_trip_member(trip_id));
create policy members_insert_self_owner on public.trip_members for insert with check (
  (user_id = auth.uid() and role = 'owner'
    and exists (select 1 from public.trips t where t.id = trip_id and t.owner_id = auth.uid()))
  or (user_id = auth.uid() and role = 'editor'
    and exists (select 1 from public.trip_invites i
                where i.trip_id = trip_members.trip_id and i.accepted_by = auth.uid() and i.expires_at > now()))
);
create policy members_delete on public.trip_members for delete using (is_trip_owner(trip_id) or user_id = auth.uid());
create policy invites_select on public.trip_invites for select using (is_trip_member(trip_id));
create policy invites_insert on public.trip_invites for insert with check (is_trip_owner(trip_id) and created_by = auth.uid());
create policy invites_delete on public.trip_invites for delete using (is_trip_owner(trip_id));
create or replace function public.accept_trip_invite(p_token text)
returns uuid language plpgsql security definer set search_path = public as $$
declare v_invite public.trip_invites;
begin
  select * into v_invite from public.trip_invites where token = p_token and expires_at > now() and accepted_by is null;
  if not found then raise exception 'invite_invalid'; end if;
  update public.trip_invites set accepted_by = auth.uid() where id = v_invite.id;
  insert into public.trip_members (trip_id, user_id, role) values (v_invite.trip_id, auth.uid(), 'editor') on conflict do nothing;
  return v_invite.trip_id;
end;
$$;
create policy bc_all on public.budget_categories for all using (is_trip_member(trip_id)) with check (is_trip_member(trip_id));
create policy bi_all on public.budget_items for all using (is_trip_member(trip_id)) with check (is_trip_member(trip_id));
create policy se_all on public.savings_entries for all using (is_trip_member(trip_id)) with check (is_trip_member(trip_id));
create policy cl_all on public.checklists for all using (is_trip_member(trip_id)) with check (is_trip_member(trip_id));
create policy cli_all on public.checklist_items for all
  using (is_trip_member((select trip_id from public.checklists c where c.id = checklist_id)))
  with check (is_trip_member((select trip_id from public.checklists c where c.id = checklist_id)));
create policy itd_all on public.itinerary_days for all using (is_trip_member(trip_id)) with check (is_trip_member(trip_id));
create policy its_all on public.itinerary_slots for all
  using (is_trip_member((select trip_id from public.itinerary_days d where d.id = day_id)))
  with check (is_trip_member((select trip_id from public.itinerary_days d where d.id = day_id)));
create policy aic_all on public.ai_conversations for all using (is_trip_member(trip_id)) with check (is_trip_member(trip_id));
create policy aim_all on public.ai_messages for all
  using (is_trip_member((select trip_id from public.ai_conversations c where c.id = conversation_id)))
  with check (is_trip_member((select trip_id from public.ai_conversations c where c.id = conversation_id)));
create policy usage_own on public.ai_usage for all using (user_id = auth.uid()) with check (user_id = auth.uid());
create policy ent_select_own on public.entitlements for select using (user_id = auth.uid());
create policy templates_read on public.checklist_templates for select using (true);
create policy paises_read on public.paises_visto for select using (true);
insert into public.paises_visto (pais_iso, pais_nome, exige_visto_br, tipo_visto) values
  ('US', 'Estados Unidos', true,  'B1/B2'),
  ('CA', 'Canadá',         true,  'Visto/eTA'),
  ('AU', 'Austrália',      true,  'Visitor'),
  ('CN', 'China',          true,  'L'),
  ('IN', 'Índia',          true,  'e-Visa'),
  ('JP', 'Japão',          false, null),
  ('GB', 'Reino Unido',    false, null),
  ('PT', 'Portugal',       false, null),
  ('FR', 'França',         false, null),
  ('IT', 'Itália',         false, null),
  ('ES', 'Espanha',        false, null),
  ('AR', 'Argentina',      false, null),
  ('CL', 'Chile',          false, null),
  ('MX', 'México',         false, null);
insert into public.checklist_templates (tipo, titulo, tier, marco, ordem) values
  ('documentos', 'Passaporte válido (mínimo 6 meses após o retorno)', 'free', 90, 1),
  ('documentos', 'Passagens aéreas confirmadas', 'free', 60, 2),
  ('documentos', 'Seguro viagem contratado', 'free', 30, 3),
  ('documentos', 'Comprovante de hospedagem impresso/salvo', 'free', 15, 4),
  ('documentos', 'Cópias digitais dos documentos na nuvem', 'free', 7, 5),
  ('preparativos', 'Avisar o banco sobre a viagem', 'free', 15, 1),
  ('preparativos', 'Contratar chip internacional ou eSIM', 'free', 15, 2),
  ('preparativos', 'Verificar necessidade de vacinas', 'free', 60, 3),
  ('preparativos', 'Fazer câmbio / cartão internacional', 'free', 30, 4),
  ('mala', 'Roupas para o clima do destino', 'free', null, 1),
  ('mala', 'Adaptador de tomada universal', 'free', null, 2),
  ('mala', 'Medicamentos de uso contínuo + receitas', 'free', null, 3),
  ('mala', 'Carregadores e power bank', 'free', null, 4),
  ('compras', 'Mala/bagagem adequada às regras da cia aérea', 'free', null, 1),
  ('compras', 'Itens de higiene em tamanho de viagem', 'free', null, 2);
```
---
## 5. Fases (agrupamento macro dos tickets da seção 7)
1. **Fundação**: schema acima aplicado, rotas `/trip/*`, bottom nav, `trip-math` com testes, dashboard, wizard + clonagem de templates
2. **Financeiro**: economia mensal, orçamento por categoria, moeda dual
3. **Checklists**: 4 listas, banco de templates completo, item Visto contextual
4. **Roteiro + PDF**: dias/slots, export client-side
5. **Monetização**: entitlements, Stripe, paywall único, bônus Pro+/Vip+
6. **Colaboração + IA**: convites, membros, assistente com cotas
7. **Polimento**: PWA, instrumentação (Sentry/PostHog), LGPD, QA mobile, modo concluída + NPS
---
## 6. Protocolo de execução por ticket
1. Confirme que o ticket está desbloqueado (seção 0)
2. Antes de abrir uma issue nova para um ticket, busque no GitHub por issues/PRs existentes com o código do ticket no título (ex.: "VJT-003"). Se já existir, NÃO crie uma nova — continue a existente ou pare e avise, não duplique o trabalho
3. Abra uma issue no GitHub (repositório `mzinhoww-svg/viaja-junto-comigo`) com o corpo do ticket (título, problema, escopo IN/OUT, comportamento esperado, arquivos afetados, acceptance criteria, cenários de teste, rollout/kill switch se houver, eventos/métricas)
4. Crie branch a partir de main atualizada: `claude/vjt-xxx`
5. **Contrato antes do código**: defina assinaturas/tipos/erros da parte nova antes de implementar
6. Implemente SOMENTE o escopo do ticket. Escreva os testes no mesmo commit/PR (nunca em ticket separado)
7. QA manual simulado: percorra cada acceptance criterion como a persona P1 faria, em viewport mobile; verifique estados vazio/carregando/erro/sucesso
8. Rode lint, typecheck, testes e build localmente antes do push
9. Commit, push, abra PR contra main referenciando `Closes #N` da issue
10. Atualize a seção 0 (ticket em aberto aguardando review) e adicione entrada na seção 8 com: o que foi feito, arquivos tocados, tabelas/RLS tocadas, como testar no celular, link do PR
11. **Execução em paralelo**: se este ticket rodar simultaneamente com outros da mesma onda (sessões/ambientes separados), NÃO edite a Seção 0 nem a Seção 8 deste arquivo no seu PR — apenas descreva o resultado completo no corpo do PR e da issue. A consolidação da Seção 0 e do Log em VIAJALY-TRIP.md acontece em uma única sessão de fechamento de onda, depois que todos os PRs da onda estiverem mergeados na main
12. Pare. Não mergeie. Não inicie o próximo ticket até essa entrada ser seguida de "Mergeado" no log (o humano avisa, ou você confere o status da issue/PR na próxima sessão)

**Regra geral, vale para toda sessão, não só execução de ticket**: nunca afirme o estado de um PR/issue/branch de memória ou por inferência de contexto anterior — sempre confirme contra a API do GitHub antes de declarar "mergeado", "aberto", "draft" etc. Se a última verificação real for antiga, reverifique antes de afirmar.

**CI verde precisa ser do commit que vai efetivamente para a main.** Se o PR receber "Update branch" (ou merge de main na branch) depois do último run verde, o commit de merge resultante precisa de CI verde próprio antes do merge — o verde anterior não vale, porque a combinação dos dois lados nunca foi testada. Isso vale mesmo quando o git auto-mergeia sem conflito: regiões adjacentes de um mesmo arquivo podem produzir código inválido sem gerar marcador de conflito (achado real: PR [#67](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/67) + [#68](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/68) quebraram a main em 2026-07-25; conserto no PR [#70](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/70)).

### Revisor automatizado
Além do CI, todo PR passa por uma sessão/rotina separada de revisão (Claude Code Remote, gatilho em PR aberto/sincronizado, nunca em comentário criado, para evitar auto-resposta). O revisor NUNCA implementa nem faz merge — só comenta no PR com achados concretos, sempre contra o diff real, nunca contra o resumo do próprio PR. Checklist obrigatório do revisor, nesta ordem:
1. Toda fórmula/agregação nova reutiliza trip-math.ts (ou o módulo puro equivalente do domínio), ou reimplementa em paralelo?
2. Se o PR toca dados que outra tela/hook também lê (ex.: budget_items, trips.cambio_manual), existe invalidação cruzada entre as queries, testada de verdade (não só argumentada)?
3. Existe issue/PR duplicado para o mesmo ticket?
4. Alguma dependência de backlog ou escopo de arquivo foi adicionada sem necessidade técnica real?
5. Edge cases da Seção 2 (modo sonho, concluída, meta zero, estouro, divisão por zero) cobertos quando o ticket os toca?

O implementador responde aos comentários do revisor na mesma branch, corrige, empurra novo commit, e pede nova revisão. Máximo 2 rodadas de revisor↔implementador; na 3ª rodada sem fechamento, o revisor para e escala para o humano com um resumo do que ainda diverge. Merge continua exclusivamente humano em qualquer cenário — o revisor não tem, e nunca deve ter, permissão de merge.
---
## 7. Backlog de tickets (27, com dependências)
Formato: **VJT-XXX Título** — Problema · Escopo IN/OUT · Aceite · Bloqueado por.
**VJT-001 ⛁ Fundação do repositório** — Sem base nada roda. IN: aplicar o schema da seção 4 no Supabase, criar rotas `/trip/*`, bottom nav, CI (lint+typecheck+testes+build), configurar Sentry. OUT: qualquer tela interna. Aceite: preview funcional (o ticket dizia "preview Vercel"; o deploy real é **Lovable** — ver Seção 3); tabelas/seeds visíveis no Supabase; CI rodando. Bloqueado por: —
**VJT-002 trip-math + testes** — Fórmulas centralizadas evitam divergência. IN: módulo puro com todas as fórmulas da seção 2 + testes de todos os edge cases. OUT: UI. Aceite: 100% dos casos com teste no CI. Bloqueado por: VJT-001
**VJT-003 ⛁ Wizard de onboarding + clonagem de templates** — Sem trip não há produto. IN: wizard 4 passos (destino, data/modo sonho, viajantes, orçamento opcional), banco completo de templates (premium 80-120 itens + packs Orlando/Europa + variáveis), motor de clonagem por tier/variáveis. Aceite: trip criada em < 60s; checklists corretos para 2 combinações de variáveis. Bloqueado por: VJT-001
**VJT-003b Completar catálogo premium genérico para a faixa 80-120 itens conforme PRD** — O PR #19 (VJT-003) usa corretamente as variáveis `regiao`/`clima`/`com_crianca`/`destino_pack`, mas o bucket premium genérico fica raso: um usuário premium sem pack de destino correspondente (ex. Ásia, América do Sul) recebe hoje bem menos que a faixa 80-120 itens da Seção 2 — os packs Orlando/Europa cobrem o gap só para quem viaja para lá. IN: migration aditiva ampliando somente o bloco premium genérico (sem condição de `regiao`/`clima`/`destino_pack`) até fechar 80-120 itens totais para um usuário premium sem pack correspondente. OUT: novos packs de destino, novas variáveis, mudanças no motor de seleção (`trip-templates.ts`) ou no wizard. Aceite: cenário premium sem pack (ex. destino no Japão ou Chile) soma entre 80 e 120 itens no catálogo aplicável; teste de contagem cobrindo o cenário. Bloqueado por: VJT-003 (PR #19) — **próximo da fila assim que VJT-003 mergear**, pois bloqueia VJT-007 e VJT-009 (dependem de checklists completos para UI e regra de aceite)
**VJT-003c Validação de data passada no wizard de criação de trip** — `determinarModoTrip` (VJT-002, já mergeado e testado) já trata corretamente data no passado retornando `concluida`, mas o wizard de criação (VJT-003/PR #19) não impede nem avisa o usuário que escolhe uma data de viagem no passado — hoje isso cria a trip silenciosamente já em modo "concluída", sem nenhuma mensagem. IN: validação em `validarNovaTripInput` (`src/lib/trip-wizard.ts`) rejeitando (ou pedindo confirmação explícita para) data de viagem anterior à data atual no passo 2 do wizard. OUT: qualquer mudança em `trip-math.ts`/`determinarModoTrip` (já corretos, não tocar). Aceite: selecionar uma data passada no wizard produz mensagem de erro clara (ou confirmação explícita) em vez de criar silenciosamente uma trip "concluída"; teste unitário cobrindo o cenário. Bloqueado por: —
**VJT-004 Dashboard Sua Jornada** — Tela-âncora de retorno. IN: progresso combinado, countdown editável, stepper com critérios, atalhos, empty states; consome trip-math. Aceite: estados sonho/planejando/concluída corretos. Bloqueado por: VJT-002, VJT-003
**VJT-004b Extrair `MARCOS_CONHECIDOS` para módulo compartilhado** — O PR #25 (VJT-004) declara `MARCOS_CONHECIDOS = [90,60,30,15,7]` localmente em `src/lib/trip-journey.ts`; não é duplicação hoje (nada mais usa essa lista ainda), mas VJT-007 (UI de checklists) vai precisar do mesmo conjunto de marcos para agrupar itens por prazo, e duas constantes independentes divergem se um marco mudar no futuro. IN: mover a constante (e `labelMarco`, se fizer sentido) para um local compartilhado (ex. `trip-math.ts` ou um novo módulo de constantes de domínio) e reexportar de `trip-journey.ts`, sem alterar comportamento. OUT: qualquer nova lógica de negócio ou mudança nos marcos em si. Aceite: `trip-journey.ts` e o futuro consumo em VJT-007 importam da mesma fonte; teste garante que não há segunda lista redeclarada. Bloqueado por: VJT-004 (mergeado) — **próximo da fila**, pois bloqueia VJT-007
**VJT-005 Economia mensal** — Motor de hábito mensal. IN: CRUD de registros, cards (meses restantes, sugestão, total), barra combinada, dica dinâmica. Aceite: valores batem com trip-math em cenário documentado. Bloqueado por: VJT-004
**VJT-006 Orçamento por categoria + moeda dual** — Orçamento real vive em duas moedas. IN: categorias default + CRUD, itens, donut, falta pagar, badge de estouro, câmbio manual com recálculo e aviso. Aceite: item em USD consolida em BRL; alterar câmbio recalcula tudo. Bloqueado por: VJT-004
**VJT-007 UI de checklists** — Dá interface aos dados clonados. IN: 4 listas, CRUD, marcos 90/60/30/15/7, progresso por lista e global. Aceite: marcar item atualiza o dashboard sem reload. Bloqueado por: VJT-003, VJT-003b (catálogo premium precisa fechar a faixa 80-120 antes da UI ser considerada "dados completos"), VJT-004b (evitar redeclarar `MARCOS_CONHECIDOS` — reaproveitar a constante compartilhada)
**VJT-007b Consolidar chave de invalidação do Dashboard em `useTripChecklists.ts`** — VJT-007 (PR #39) invalida `["trip","dashboard",tripId]` como literal local em `invalidateChecklists`, em vez de importar `tripDashboardQueryKey` de `trip-query-keys.ts` (módulo criado no VJT-006b/PR #38, especificamente para eliminar esse tipo de duplicação — os dois tickets rodaram em paralelo e não convergiram). O valor bate hoje, mas é duplicação, não fonte única. IN: importar `tripDashboardQueryKey` (e, se fizer sentido, mover `invalidateChecklists` para o mesmo padrão de `invalidateFinanceiro`) em `useTripChecklists.ts`; adicionar teste de regressão para a invalidação cruzada checklists→dashboard, no mesmo padrão do teste criado em VJT-006b (`src/hooks/trip-financeiro-invalidation.test.ts`) para budget/savings/dashboard. OUT: qualquer mudança de comportamento visível ou de fórmula. Aceite: `useTripChecklists.ts` não declara mais a chave do dashboard como literal; teste de regressão cobrindo toggle/add/update/delete de item invalidando checklists+dashboard. Bloqueado por: — (sem bloqueio; PR #39 já mergeado)
**VJT-008 Roteiro dias e slots** — Módulo de maior uso perto da viagem. IN: dias auto-gerados, slots Manhã/Tarde/Noite, adicionar/remover/duplicar/reordenar/colapsar, limite free 5 dias (ponto plugável). Aceite: roteiro de 15 dias editável no celular. Bloqueado por: VJT-001
**VJT-009 Item de visto contextual** — Funil para a consultoria, sem bloquear. IN: item via `paises_visto`, card com UTM, evento `visa_cta_click`. Aceite: trip EUA exibe o card; trip Portugal não. Bloqueado por: VJT-007
**VJT-010 Export PDF do roteiro** — Gatilho de upgrade + utilidade offline. IN: PDF client-side com capa da marca, resumo e dias; botão condicionado a entitlement (mock até VJT-011). Aceite: PDF de 15 dias baixável no celular. Bloqueado por: VJT-008
**VJT-011 ⛁ Entitlements + paywall único** — Fonte única de plano. IN: hook de entitlement, modal único, 6 gatilhos aplicados, ativação manual/admin do bônus Pro+/Vip+. Aceite: free bate em todos os gatilhos implementados; flag manual libera tudo sem reload (com backstop se o canal cair — ver ressalva na Seção 8). Bloqueado por: VJT-005, VJT-006, VJT-009, VJT-010
**VJT-011b Runbook operacional de ativação manual de entitlements + contagem de itens premium do checklist por variáveis reais da trip** — Duas pendências deixadas pelo VJT-011, achadas em review humana do PR #51: (1) a ativação manual/admin do bônus Pro+/Vip+ (`UPDATE entitlements SET plano='premium', origem='manual'/'pacote_visto' WHERE user_id=...` via SQL Editor/service role) está documentada só em comentários soltos no código (`useEntitlement.ts`, migration), sem um runbook operacional para quem vai efetivamente rodar isso (ex.: time de consultoria/suporte) — falta passo a passo, incluindo como achar o `user_id` certo e como confirmar que a ativação colou; (2) o teaser premium do checklist (`usePremiumChecklistCounts`/`contarTemplatesPremiumPorTipo`, gatilho "abrir item de checklist premium") conta `checklist_templates` de tier premium pelo catálogo inteiro por tipo, não filtrado pelas variáveis reais da trip (`regiao`/`clima`/`destinoPack`/`comCriancas` — mesmas usadas por `templateAplica` em `trip-templates.ts` na hora da clonagem), então o número "+N itens completos" pode superestimar o que a trip específica realmente ganharia ao virar premium. IN: (a) runbook curto (pode viver neste próprio arquivo ou em `docs/`) com o passo a passo de ativação manual; (b) `usePremiumChecklistCounts` passa a receber as variáveis da trip e filtrar a contagem com a mesma lógica de `templateAplica`, sem duplicá-la. OUT: qualquer mudança de UI além do número exibido; checkout Stripe (VJT-012, que resolve a ativação "de verdade" via automação). Aceite: runbook publicado e referenciado a partir do código; contagem do teaser bate com o que `selecionarTemplates` selecionaria de fato para aquela trip em pelo menos 2 cenários de teste. Bloqueado por: — **✅ MERGEADO** (issue [#73](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/73), PR [#75](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/75), 2026-07-25T22:02:36Z).

**VJT-011b-1 Runbook de ativação alinhado ao mecanismo que existe hoje** (issue [#76](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/76)) — Follow-up do VJT-011b, aberto por um acidente de cronologia: o PR #75 mergeou às 22:02:36Z e o PR #72 (VJT-011c + VJT-011d) às 22:03:17Z, **41 segundos depois**, introduzindo o código de acesso admin. O runbook nasceu obsoleto — descreve o SQL Editor como *o* procedimento, quando o caminho do dia a dia passou a ser digitar um código em `/trip/mais` ou entrar por `/trip/login`. O próprio `trip-admin-code.ts` já dizia "substitui, no dia a dia, o runbook manual do VJT-011b", sem o runbook corresponder. IN: reescrever `docs/runbook-ativacao-manual-entitlements.md` com os dois caminhos e uma tabela de decisão; documentar os 5 casos que o código **não** cobre (ativar para terceiro, `origem = 'pacote_visto'`, `expires_at` com prazo, revogação, reativar `pacote_visto` expirado sem perder a linhagem), a configuração real (`TRIP_ADMIN_CODE`/`TRIP_ADMIN_EMAIL`, desligado por padrão) e a trilha em `audit_log`; fechar os ponteiros código ↔ runbook; teste guardando "premium por `pacote_visto` válido não é rebaixado nem tem a origem trocada". OUT: fazer o código de acesso aceitar `pacote_visto`/`expires_at`/revogação (mudaria o VJT-011c); mexer na contagem do checklist (já correta na `main`); checkout Stripe. Aceite: o runbook descreve o mecanismo que existe hoje, não o que existia quando o VJT-011b foi escrito. Bloqueado por: —
**VJT-011c Código de acesso admin (ativação de Premium sem SQL Editor)** — Pedido do dono do produto: tornar a ativação manual do VJT-011b ágil o bastante para uso diário (QA interno, demo para cliente, bônus Pro+/Vip+ da consultoria), sem abrir o SQL Editor e sem descobrir o `user_id` de ninguém. IN: código único configurado por variável de ambiente **só do servidor** (`TRIP_ADMIN_CODE`, sem prefixo `VITE_`, aceitando lista separada por vírgula para rotação), server fn autenticada que confere o código e ativa `plano='premium', origem='manual'` para **quem digitou** (o próprio usuário logado, nunca um terceiro), campo em `/trip/mais`, bloqueio por tentativas e registro em `audit_log`. OUT: painel de admin, ativação em nome de outro usuário, código por usuário/uso único com tabela própria, qualquer mudança no checkout do Stripe. Aceite: com a env configurada, código certo libera todos os gatilhos de paywall sem reload; código errado não vaza nada e bloqueia após 5 tentativas; sem a env, o recurso fica desligado; quem já é Premium por Stripe não tem a `origem` sobrescrita. Bloqueado por: —
**VJT-011d Login próprio do Trip (produto vendido separado) + acesso da equipe por código** — Decisão do dono do produto: o Trip é vendido apartado da consultoria de vistos, então mandar quem comprou só o Trip para `/portal/login` ("sua consultora preparou tudo pra sua viagem aos EUA", código de 6 dígitos que ele nunca recebeu) é um beco sem saída — e foi o que travou o próprio dono ao tentar entrar. IN: rota `/trip/login` com identidade do Trip, Google Sign-In como caminho principal (provider habilitado no painel), link mágico por e-mail com **cadastro aberto** (`shouldCreateUser: true`, o oposto do portal), reaproveitamento da conta de quem já é cliente Viajaly (mesmo projeto Supabase = mesma conta, sem fluxo de SSO próprio), e entrada da equipe pelo mesmo código do VJT-011c. OUT: projeto Supabase separado para o Trip, migração de base, login por senha, cobrança/planos (já cobertos pelo VJT-012). Aceite: quem nunca foi cliente de visto cria conta e entra sem passar por nenhuma tela da consultoria; quem já é cliente entra na mesma conta usando o mesmo e-mail; sessão ativa do portal continua valendo em `/trip/*`; código da equipe entra sem depender de e-mail. Bloqueado por: VJT-011c
**VJT-012 Checkout Stripe + webhook** — Receita direta. IN: checkout one-time (97/67), webhook → entitlement, idempotência por payment_id, kill switch `PAYMENTS_ENABLED`. Aceite: compra teste ativa premium sem reload; evento duplicado não duplica entitlement. Bloqueado por: VJT-011
**VJT-013 Convites e membros** — Diferencial da persona P2. IN: link mágico (7 dias), RPC `accept_trip_invite`, gestão de membros, limites por plano, kill switch `INVITES_ENABLED`. Aceite: 2 contas na mesma trip; free não convida; RLS testada contra não-membro. Bloqueado por: VJT-011
**VJT-014 Assistente IA** — Diferencial premium e motor de conversão. IN: Edge Function (OpenRouter + DeepSeek — ver Seção 3; a versão original deste ticket dizia Anthropic/Haiku, trocado por custo depois do merge do PR #61), contexto da trip, cotas 10/100 com contador, escopo fechado (recusa fora do tema, redireciona visto com UTM), kill switch `AI_ENABLED` + teto diário global. Aceite: resposta usa dados reais da trip; msg 11 no free dispara paywall. Bloqueado por: VJT-011
**VJT-015 Instrumentação PostHog** — Sem eventos não há métrica. IN: PostHog integrado, 12 eventos (signup, trip_created, checklist_item_done, savings_entry_created, budget_item_created, ai_message_sent, upgrade_view, upgrade_purchase, visa_cta_click, invite_sent, invite_accepted, pdf_exported), funis grátis→compra e visto→lead. Aceite: cada evento dispara uma vez no fluxo certo. Bloqueado por: VJT-011
**VJT-016 PWA** — Instalável no celular. IN: manifest, ícones, splash. Aceite: instalado em dispositivo real. Bloqueado por: VJT-011
**VJT-017 LGPD + modo concluída + NPS** — Fecha o ciclo do produto. IN: consentimento, exclusão de conta (cascade), modo viagem concluída com NPS e CTA nova viagem. Aceite: exclusão remove dados; data passada ativa o modo. Bloqueado por: VJT-011
**VJT-017b Nomear subprocessadores no consentimento LGPD + gate sensível à versão** (issue [#65](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/65)) — A troca do provider de IA para OpenRouter + DeepSeek (PR #63) faz o contexto da trip (destino, datas, nº de pessoas, progresso, e o texto livre digitado no chat) transitar por **dois terceiros novos**. Auditoria do que o VJT-017 entregou achou dois gaps: (1) `LGPD_CONSENT_TEXT` é genérico e a seção Compartilhamento de `/privacidade` não nomeia ninguém — e suas categorias ("hospedagem, mensageria") **não descrevem inferência de IA**, então a categoria de tratamento nem existe; (2) `useLgpdConsent` decide só por existência de linha (`return !!data`) e **nunca lê `versao_termos`** — que é gravado no insert e ignorado na leitura — com `user_id` como PRIMARY KEY de `user_lgpd_consents`, logo sem histórico. Consequência: subir `LGPD_CONSENT_VERSION` **não re-apresenta o aceite para quem já consentiu**, só para usuários novos — a população errada. Por isso "só editar a string" produz falsa sensação de conformidade. IN: nomear subprocessadores e a finalidade de IA no consentimento e em `/privacidade`; comparar `versao_termos` com a versão corrente no gate; decidir a forma de registro do re-consentimento (PK atual impede log append-only, o desejável para auditabilidade — exige migration); bumpar para `v2`. OUT: qualquer mudança na Edge Function/provider (isso é o PR #63); reescrita geral da Política de Privacidade. **Decisão do dono do produto, não do agente**: forçar ou não o re-consentimento de usuários existentes. Aceite: consentimento e `/privacidade` nomeiam os subprocessadores e descrevem o tratamento por IA; quem consentiu em `v1` é re-apresentado ao aceite quando a versão sobe (com teste). **Decisões tomadas na execução (dono do produto)**: re-consentimento forçado da base existente (hoje ~zero — custo nulo agora, alto depois); registro vira log append-only (PK própria + unique `(user_id, versao_termos)`, sem policy de update/delete); subprocessadores nomeados: Supabase e Stripe (EUA), OpenRouter (EUA, roteamento) e DeepSeek (China, inferência). **Bloqueia o deploy da Edge Function `ai-chat`**. Bloqueado por: —
**VJT-018 QA mobile e performance** — Definition of done do milestone. IN: jornada P1 completa em dispositivo real, dashboard < 2s em 4G, correções pontuais. Aceite: todos os critérios de aceite anteriores re-verificados numa passada só. Bloqueado por: VJT-012, VJT-013, VJT-014, VJT-015, VJT-016, VJT-017
**VJT-019 Auditoria final do milestone** — Fecha o v1. IN: checklist cruzado desta seção 7 inteira contra o app publicado. Aceite: os 18 tickets anteriores confirmados em produção. Bloqueado por: VJT-018
---
## 8. Log de execução
> Toda tarefa concluída gera uma entrada aqui, no topo (mais recente primeiro). Sem entrada = tarefa não existe.

### 2026-07-26 — Fechamento do milestone v1: limpeza de PRs, correção da exclusão de conta e reconciliação do documento — **Concluído** (issue [#82](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/82), PRs [#83](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/83), [#84](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/84))

- **⚖️ Autorização excepcional, registrada a pedido do dono do produto**: nesta sessão ele autorizou **mergear sem revisão humana prévia** os PRs #77, #79, #81, #83 e #84, desde que com **CI verde no head commit de cada um**. É exceção explícita e datada ao passo 12 da Seção 6 ("Nunca faça merge de PR. Essa ação é sempre humana"). Justificativa dada por ele: **base de usuários zero, PRs majoritariamente markdown, e necessidade de fechar o milestone**. **Não é precedente** — a regra volta a valer integralmente a partir da próxima sessão. Cada merge foi precedido de verificação do CI via API; nenhum PR foi mergeado com check vermelho ou pendente.

- **Fase 1 — os 4 PRs parados foram resolvidos.** Ordem escolhida para evitar merge sujo: primeiro os que não tocam `VIAJALY-TRIP.md` (#83, #81), depois os que tocam (#77, #79), com atualização de branch e **CI novo** entre eles — a Seção 6 exige que o verde seja do commit que efetivamente vai para a `main`, e o verde anterior de #77/#79 tinha sido medido contra uma base que deixou de existir.
  - **#83** (auditoria em três camadas) e **#81 (VJT-018 QA mobile)** — mergeados. O diff do #81 foi lido antes: é **só polimento** (label como alvo de toque de 16px → 247×52px, `min-w-0` em dois campos, quebra do valor no card de sugestão, `Close` → `Fechar` em `dialog.tsx`/`sheet.tsx`, e o teste que acompanha a copy). Nenhuma mudança estrutural e nenhuma decisão de produto, então mergeado conforme o critério combinado.
  - **#77 (VJT-011b-1, runbook)** — mergeado. **#79 (VJT-019, auditoria final)** — mergeado depois de resolver conflito em `VIAJALY-TRIP.md`, **preservando os dois lados**: a Seção 0 do #79 (deduplicada) mais a linha de `TRIP_ADMIN_CODE`/`TRIP_ADMIN_EMAIL` que só existia no lado do #77, e **as duas entradas de Log** (VJT-019 e VJT-011b-1), nenhuma descartada.
  - **#71 — fechado sem merge**, com a recomendação registrada no próprio PR. Obsolescência **verificada, não presumida**: o arquivo que ele conserta já está corrigido na `main` (`ai-chat.functions` mockado: 1 ocorrência; `functions.invoke`: 0), pelo merge `3ec1e51` via PR #72. O segundo achado dele (2190 erros de `prettier`) foi resolvido pelo PR #74. **O que sobrevive do #71 é a pergunta que ele levantou** sobre o editor Lovable empurrar direto para a `main` — registrada na Seção 3.

- **Fase 2 — exclusão de conta (LGPD, VJT-017) consertada** (PR #84). A Edge Function `delete-account` respondia **404** em produção desde o merge do VJT-017: o botão estava publicado chamando um endpoint inexistente.
  - **A hipótese de causa estava errada, e vale registrar.** Suspeitava-se do mesmo motivo da `ai-chat` (bundler não empacota `src/lib/*`). **Não era**: `supabase/functions/delete-account/index.ts` importava só de `esm.sh`, sem nenhum import local — exatamente como as duas functions que **estão** deployadas. A causa real é mais simples e pior: **não existe pipeline que deploye Edge Functions neste repositório** (nenhum workflow menciona Supabase). As duas que funcionam foram deployadas por fora, em algum momento; a que nasceu no VJT-017, nunca.
  - **O remédio pedido continuou certo, por um motivo mais forte que o diagnóstico**: enquanto o deploy for manual, toda function nova nasce com a mesma chance de ficar 404 em silêncio. Migrada para `createServerFn` (`src/lib/delete-account.functions.ts` + `delete-account.server.ts`), que sobe junto com o app.
  - **Preservado**: service role (única forma de excluir do Auth); o **cascade das FKs como fonte única** da árvore de exclusão — o handler faz **uma** chamada, `auth.admin.deleteUser()`, sem nenhum `DELETE` manual em TypeScript; a confirmação digitando "EXCLUIR". **Nenhum módulo compartilhado foi vendorizado** — `supabaseAdmin` foi reusado, com **import dinâmico dentro do handler**, porque `*.functions.ts` vai para o bundle do client e um import de topo arrastaria a chave de service role para o navegador.
  - **Verificado no build, não argumentado**: varredura dos 180 chunks de `.output/public` — **0 ocorrências** de `SERVICE_ROLE`, `service_role`, `SUPABASE_SERVICE`, `admin.deleteUser` e `delete-account.server`. E o handler aparece registrado no resolver do TanStack ao lado de `redeemAdminCode` e `sendAiChatMessage`, que já funcionam em produção.
  - **Testes (524 → 545)**: 4 no hook (incluindo um que quebra se alguém adicionar parâmetro de `user_id` — a exclusão é sempre do usuário do JWT), 3 no módulo de servidor, e **12 de contrato sobre as migrations**: o que o cascade remove (trips e todo o conteúdo, `ai_conversations`, `trip_invites`, `trip_nps_responses`, `ai_usage`, `entitlements`, `user_lgpd_consents`) e o que **sobrevive anonimizado** (`savings_entries.created_by` é `SET NULL` e **não** cascade, a coluna aceita NULL, e `valor_brl_cents` não é tocado). Limitação declarada no topo do arquivo de teste: **não há Postgres neste ambiente** (o binário do `docker` existe, mas o daemon não sobe), então o teste verifica o que as migrations **declaram** — precedente já usado em `vjt017b-migration-consent-history.test.ts`. O estado **real** foi conferido em separado, por consulta a `pg_constraint.confdeltype` no banco de produção, e bate linha a linha.
  - **Só passa a funcionar quando a `main` for publicada.**

- **Fase 3 — documento reconciliado.** Seção 0 reescrita inteira contra estado verificado em três camadas; sem campo duplicado (a versão anterior tinha 4 pares, 3 deles contraditórios entre si).
  - **A correção de maior alcance: a hospedagem não é Vercel, é Lovable.** O documento afirmou isso por 12 dias, e o custo foi concreto — variáveis procuradas no painel errado e um atraso de publicação lido como falha de esteira da Vercel, que não existe aqui. Evidência e consequências operacionais na Seção 3. **Menções na Seção 8 (Log) foram deixadas intactas de propósito**: o Log é registro histórico do que se acreditava na época, e reescrevê-lo seria falsificar a memória do projeto — a Seção 3 instrui a lê-las como "painel do Lovable".
  - **Publicação**: a `main` foi publicada em 2026-07-26 e o item "mergeado mas não publicado" do VJT-019 está **encerrado** (`/trip/login` responde 307 → 200, era 404). Mas os merges **de hoje** ainda não estão no ar.
  - **Erro meu, corrigido antes de virar registro**: em duas medições intermediárias concluí que VJT-011b e VJT-011c/d **não** estavam publicados, porque os marcadores davam 0. Era **lacuna do meu próprio crawl** — eu não tinha baixado os chunks de `/trip/login` nem de `/trip/checklists`. Com a varredura completa (101 chunks) os três marcadores aparecem. É exatamente a armadilha que a auditoria do #83 tinha registrado ("ausência na varredura pode ser lacuna de crawl, não do build") e na qual eu mesmo caí em seguida. **Regra prática**: ausência de string só vale como prova depois de confirmar que o chunk da rota relevante foi baixado.
  - Registrados na Seção 3 os **dois desvios de processo** com mitigação: o VJT-006b mergeado em duplicidade (com o diagnóstico de que a checagem de duplicata é leitura sem trava e não protege contra concorrência) e os 8 commits do editor Lovable direto na `main` (três opções avaliadas, recomendação: **job de auto-formatação agora, branch protection só se entrar um segundo desenvolvedor** — a opção tecnicamente superior é operacionalmente errada aqui, porque tiraria o editor visual das mãos do dono do produto).

- **Estado do repositório ao fim da sessão**: `npx vitest run` **545/545** em 55 arquivos; `npm run typecheck` 0 erros; `npm run lint` 0 erros e 10 warnings (idênticos ao baseline); `npm run build` ok. As 4 rotas de MCP regeneradas pelo build foram revertidas antes do commit.

- **O que continua dependendo exclusivamente do humano** (nenhuma é código): publicar a `main`; `VITE_SENTRY_DSN`; virar o Stripe para `pk_live_`; `VITE_POSTHOG_KEY` + os 2 funis; `TRIP_ADMIN_CODE` + `TRIP_ADMIN_EMAIL`. Todas no **painel do Lovable**.

### 2026-07-25 — VJT-019 Auditoria final do milestone v1 — **PR aberto (draft), aguardando review** (issue [#78](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/78), branch `claude/v1-final-audit-s8zk4j`)

Cruzamento da Seção 7 inteira **contra o app publicado**, não contra o código. Nenhuma linha de código de produção foi tocada — este PR só audita e reescreve a Seção 0 e este Log.

**Como cada afirmação foi provada.** Quatro fontes, todas externas ao repositório:
1. **Bundles publicados** — 89 chunks JS baixados de `viajaly.com` (as páginas SSR entregam só o shell e hidratam no cliente, então o conteúdo verificável está nos chunks, não no HTML). O build da Vercel inlina o objeto `import.meta.env` inteiro, o que torna a lista de variáveis `VITE_*` de produção legível byte a byte.
2. **PostgREST com a chave anon** — existência de tabela, de coluna e de RPC, mais contagens via `Prefer: count=exact`. Cada classe de sondagem foi calibrada com **controle negativo**: tabela inexistente → `PGRST205`, coluna inexistente → `42703`, função inexistente → `PGRST202` com `hint: null`. Sem esses controles, um `200` não provaria nada.
3. **Edge Functions** — `GET /functions/v1/<nome>`, com `404 NOT_FOUND` separando não-deployada de deployada.
4. **Rotas HTTP** de produção, código de status real.

**Limitação de método, registrada por honestidade**: o Chromium do ambiente não passa pelo proxy de rede (`ERR_CONNECTION_RESET` até contra `example.com`), então **não houve sessão autenticada clicada**. Telas atrás do login foram verificadas pelo código efetivamente publicado nos chunks + suíte de testes (524/524), não por interação humana. Esse é exatamente o buraco que o VJT-018 deveria fechar.

#### Achado que muda o status do milestone: o VJT-018 nunca existiu
Zero issues e zero PRs com o código VJT-018 (`search_pull_requests` → `total_count: 0`; as 14 issues que casam com o termo só o citam no corpo copiado do backlog). O VJT-018 é a **Definition of done do milestone** na Seção 7 e o bloqueio declarado do VJT-019. Ou seja: **o v1 está com todas as features entregues e com a DoD não cumprida** — ninguém percorreu a jornada P1 num aparelho real e ninguém mediu o dashboard em 4G. A auditoria seguiu assim mesmo, por pedido explícito do dono do produto, e este parágrafo é o registro de que o pré-requisito foi pulado conscientemente.

#### Veredito ticket a ticket

**A. Entregue e funcional em produção (19)**

| Ticket | Evidência em produção |
|---|---|
| VJT-001 Fundação | 19 tabelas existem, RLS ativa (anon lê 0 linhas em todas), `paises_visto`=14, rotas `/trip/*` respondem 200, CI verde no HEAD. **Ressalva: a parte "configurar Sentry" está inerte** — ver bloco B |
| VJT-002 trip-math | Chunk `trip-math-*.js` publicado; 524 testes verdes cobrindo as fórmulas e os edge cases |
| VJT-003 Wizard | `/trip/novo` → 200; `trips.num_criancas` existe; catálogo clonável presente |
| VJT-003b Catálogo premium | `checklist_templates` = **142** em produção (faixa 80-120 aplicável por trip, conforme a Seção 2) |
| VJT-003c Data passada | Validação publicada no chunk do wizard + teste unitário verde |
| VJT-004 Dashboard | `/trip` → 200, `<title>Sua Jornada — Viajaly Trip</title>` |
| VJT-004b MARCOS_CONHECIDOS | Constante única no bundle, sem segunda lista |
| VJT-005 Economia | `/trip/financeiro` → 200; chunk `trip-savings-*.js` |
| VJT-006 Orçamento + moeda dual | "Falta pagar" e o donut no chunk `trip.financeiro-*.js` |
| VJT-006b Invalidação cruzada | Testes de regressão verdes (ticket só no Log, não na Seção 7) |
| VJT-007 UI de checklists | `/trip/checklists` → 200; teaser premium e item "Visto" no chunk |
| VJT-007b Chave de invalidação | Fonte única confirmada no bundle |
| VJT-008 Roteiro | `/trip/roteiro` → 200; slots "Manhã/Tarde/Noite" no chunk |
| VJT-009 Item de visto | `paises_visto`=14, `US` com `exige_visto_br: true`. **`link_consultoria` é `null`** → o fallback WhatsApp é o caminho único, como já decidido na Seção 3 (decisão consciente, não pendência). O evento `visa_cta_click` existe no bundle mas **não coleta nada** (ver B1) |
| VJT-010 Export PDF | `jspdf` publicado em chunk próprio, botão condicionado a entitlement |
| VJT-011 Entitlements + paywall | Tabela `entitlements` existe; **os 6 gatilhos** estão no bundle (`segunda_viagem`, `convidar_membro`, `roteiro_dia_6`, `exportar_pdf`, `cota_ia`, `checklist_premium`), todos abrindo o mesmo modal |
| VJT-013 Convites e membros | RPC `accept_trip_invite(p_token)` existe; `trip_invites` com `expires_at` de 7 dias; kill switch ausente = ligado |
| VJT-016 PWA | `/trip/manifest.webmanifest` → 200, `icon-192.png` → 200, `apple-touch-icon` e `theme-color: #10204A` no HTML. **O único ticket da onda 7 que está 100% no ar sem ressalva** |
| VJT-017b LGPD versionado | Os 4 subprocessadores nomeados com país e papel nos chunks `lgpd-consent-*.js` e `privacidade-*.js`; migration do log append-only **aplicada** |

**B. Entregue mas inerte por pendência manual (4)** — mergeado, publicado, e mesmo assim sem efeito nenhum:

1. **PostHog (VJT-015) — 0 eventos desde o merge.** O build de produção não tem `VITE_POSTHOG_KEY`: o `import.meta.env` inlinado no bundle contém **apenas** `VITE_PAYMENTS_CLIENT_TOKEN`, `VITE_SUPABASE_PROJECT_ID`, `VITE_SUPABASE_PUBLISHABLE_KEY` e `VITE_SUPABASE_URL`. O gate devolve `null`, o `posthog-js` nem chega a ser importado. Os 12 nomes de evento estão corretos no bundle — o ticket foi bem executado, só não coleta. Os 2 funis continuam por criar no painel.
2. **Sentry (VJT-001) — nenhum erro reportado.** Sem `VITE_SENTRY_DSN`, `initSentry()` retorna antes do `Sentry.init` e o SDK é removido do bundle: **0 ocorrências da string `sentry` nos 89 chunks**. Um erro em produção hoje não chega a ninguém.
3. **Stripe (VJT-012) — não cobra de verdade.** O build publicado carrega **`pk_test_...`**: o checkout roda em modo teste. Somando o `PAYMENTS_ENABLED` *fail-closed* (qualquer valor diferente de `"true"`, inclusive ausente, mantém o checkout desligado), o v1 **não tem receita ligada**. O webhook está publicado e responde (`200` com `env` inválido; `400` na verificação de assinatura) — a metade que recebe pagamento funciona, a que vende está em sandbox.
4. **Exclusão de conta (VJT-017) — quebrada, e é LGPD.** `GET /functions/v1/delete-account` → **`404 NOT_FOUND`**, enquanto `lock-usd-rate` e `refresh-usd-reference` respondem no mesmo projeto (405/502) — ou seja, o projeto deployou funções, só não essa. O botão "Excluir conta" está no chunk `trip.mais-*.js` e chama `supabase.functions.invoke("delete-account")`: o usuário clica e toma erro. O aceite do ticket ("exclusão remove dados") **não se sustenta em produção**.

**C. Mergeado mas ainda não publicado (3 tickets, 4 PRs)** — VJT-011b (PR #75), VJT-011c e VJT-011d (PR #72), mais os PRs #74 e #64. `GET /trip/login` → **404** e as strings `Acesso da equipe` / `Entrar com Google` não aparecem em nenhum dos 89 chunks. O build no ar é de **entre 19:58Z e 20:29Z**: tem VJT-015 e VJT-017b, não tem o primeiro commit do VJT-011c (`33dd7cf`). Detalhe possivelmente causal: **#75 e #74 foram mergeados com CI vermelho** (`826f512` e `e8565f3` falharam) — vale olhar a esteira de deploy da Vercel antes de concluir que é só atraso. Enquanto isso, o beco sem saída de login que motivou o VJT-011d segue exposto ao cliente que comprou só o Trip.

**D. Fora do v1 (2)** — **VJT-018** (nunca executado, ver acima) e **VJT-019** (esta auditoria, em review).

#### Definition of done do milestone (Seção 2, "Regras não negociáveis"), item a item
Nota de procedência: **não existe arquivo `REQUIREMENTS` neste repositório** (`find -iname "*requirement*"` → vazio). A DoD auditada é a Seção 2 do próprio VIAJALY-TRIP.md, que é onde as regras não negociáveis do milestone vivem, somada ao critério de aceite do VJT-018.

| Item da Seção 2 | Veredito |
|---|---|
| Fórmulas num módulo puro `trip-math` com testes | ✅ Módulo puro publicado, 524 testes verdes em 53 arquivos |
| Edge cases (sonho, concluída, meta zero, estouro, divisão por zero) | ✅ Cobertos por teste; modo concluída + NPS publicados (`trip_nps_responses` existe, textos no chunk `trip.index-*.js`) |
| Paywall: 6 gatilhos, um só modal | ✅ Os 6 no bundle, mesmo modal |
| Fonte única de plano (`entitlements` + um hook) | ✅ Tabela + chunk `entitlements-*.js`; nenhuma checagem ad hoc |
| Preço one-time R$ 97 / lançamento R$ 67 | ⚠️ Só existe `PREMIUM_PRICE_BRL_CENTS = 6700` (lançamento). O preço cheio de R$ 97 não existe como constante — hoje não faz falta, mas encerrar o lançamento exige mudança de código |
| Item Visto contextual, nunca bloqueante, com UTM | ✅ Publicado; `link_consultoria` nulo → fallback WhatsApp (decisão registrada) |
| RLS em todas as tabelas, nunca service role no client | ✅ As 19 tabelas devolvem 0 linhas para anon; **`service_role` não aparece em nenhum dos 89 chunks**; `is_trip_member`/`is_trip_owner`/`trip_owner_id` existem em produção |
| Convite por token opaco, 7 dias, aceite por RPC | ✅ `expires_at default now() + interval '7 days'`; `accept_trip_invite(p_token)` existe |
| Fora de escopo respeitado (push, split, câmbio automático, etc.) | ✅ Nada implementado fora do escopo |
| Valores em centavos, UI PT-BR, mobile-first, bottom nav de 5 itens | ✅ Confirmado no código publicado |
| **Jornada P1 completa em aparelho real** (VJT-018) | ❌ **Não feito** — ticket nunca executado |
| **Dashboard < 2s em 4G** (VJT-018) | ❌ **Não medido** |
| Observabilidade ligada (Sentry + PostHog) | ❌ **Inerte** — ver bloco B |

#### Estado do repositório nesta sessão
`bun run test` → **524/524 em 53 arquivos**. `bun run lint` → **0 erros**, 10 warnings de `react-refresh`. `bun run typecheck` → 1 erro, **causado pelo ambiente e não pelo repo**: o pacote `@lovable.dev/cloud-auth-js` não baixa aqui (`ConnectionClosed` no registry), então o módulo não resolve. O CI do GitHub, que instala normalmente, está **verde no HEAD `8ed7fc7`**.

#### O que fazer com isto (nada é código; tudo é ação manual do dono do produto)
Em ordem de dano se ficar como está:
1. **Redeployar a `main`** — devolve `/trip/login` ao ar (VJT-011b/011c/011d). Checar antes por que a esteira parou nos merges com CI vermelho
2. **Deployar a Edge Function `delete-account`** — é obrigação legal, não feature
3. **Trocar `VITE_PAYMENTS_CLIENT_TOKEN` para a chave live e ligar `PAYMENTS_ENABLED=true`** — sem isso o v1 não fatura
4. **Cadastrar `VITE_POSTHOG_KEY` e `VITE_SENTRY_DSN`** na Vercel e criar os 2 funis no PostHog — sem isso o v1 sobe sem métrica e sem alarme
5. **Executar o VJT-018 de verdade** antes de declarar o milestone fechado
6. Fechar o PR #71 (obsoleto) e apagar a branch órfã `claude/vjt-003-onboarding-wizard`
### 2026-07-25 — VJT-011b-1 Runbook de ativação alinhado ao mecanismo que existe hoje — **PR aberto (draft), aguardando review** (issue [#76](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/76), PR [#77](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/77), branch `claude/vjt-011b-premium-checklist-3h2x8e`)

- **Como esta sessão começou**: despachada para executar o **VJT-011b**. Antes de escrever qualquer código, a checagem de duplicata exigida pela Seção 6 mostrou que o ticket **já estava feito**: issue #73 fechada, PR #75 mergeado às `2026-07-25T22:02:36Z`. A Seção 0 ainda dizia "VJT-011b (sem bloqueio) segue disponível" e a Seção 8 não tinha entrada nenhuma dele — divergência entre este arquivo e o repositório, exatamente o caso que o passo 1 do protocolo manda reconciliar antes de fazer coisa nova. Reexecutar às cegas teria duplicado trabalho já mergeado.
- **O que estava mesmo pendente**: o PR #72 (VJT-011c + VJT-011d) mergeou às `22:03:17Z` — **41 segundos depois** do #75. O runbook do VJT-011b, escrito antes disso, ficou obsoleto no minuto seguinte ao merge: descrevia o SQL Editor como *o* procedimento de ativação, sem mencionar que existe um código de acesso que resolve o mesmo problema em 10 segundos. Quem abrisse o documento hoje abriria o SQL Editor sem saber. Escopo desta sessão ajustado para isso — documentar o mecanismo que **de fato existe**, não o que existia quando o ticket foi escrito.
- **Metade (2) do VJT-011b conferida linha a linha, não assumida**: `contarTemplatesPremiumPorTipo` filtra por `templateAplica` com as variáveis reais da trip (reuso, sem reimplementar a regra); `useTripVariables` busca **pelo `tripId` da rota**, não pela primeira trip do usuário; `usePremiumChecklistCounts` cacheia pela combinação de variáveis. Testes em `usePremiumChecklistCounts.test.tsx`, `trip-checklists.test.ts` e `trip-destinations.test.ts` passando. **Nada a corrigir** — a contagem premium não foi tocada nesta sessão.
- **Achado que motiva metade do runbook novo**: o código de acesso **não substitui o SQL inteiro**, ao contrário do que o comentário de `trip-admin-code.ts` sugeria ("substitui, no dia a dia, o runbook manual"). Lendo `premium-entitlement.server.ts` e `trip-admin-code.functions.ts`, sobram 5 casos só possíveis por SQL: (1) ativar para um cliente que não vai receber o código — a server fn não aceita `user_id`, de propósito; (2) gravar `origem = 'pacote_visto'`, que o código nunca faz (grava sempre `manual`), **então ativar o bônus Pro+/Vip+ por código apaga do registro que ele veio de um pacote da consultoria**; (3) `expires_at` com prazo (o código grava sempre `null`); (4) revogar; (5) reativar um `pacote_visto` expirado sem perder a linhagem. O runbook agora abre com a tabela de decisão entre os dois caminhos, em vez de tratar um como substituto do outro.
- **Contrapartida a favor do código**: ele é o único caminho que deixa rastro — `trip_admin_code_redeemed`/`trip_admin_login` em `audit_log`. O SQL não registra quem rodou o quê, só o `updated_at` da linha. O runbook traz a query de auditoria.
- **Alerta operacional novo no runbook**: o código é **credencial de equipe, não cupom de cliente** — quem recebe libera o próprio Premium e pode repassá-lo adiante. Para dar Premium a um cliente, o caminho certo continua sendo o SQL, que ativa a conta dele sem entregar credencial nenhuma.
- **Arquivos**: reescrito `docs/runbook-ativacao-manual-entitlements.md` (Caminho A = código, Caminho B = SQL, tabela de decisão, configuração de ambiente, auditoria, diagnóstico com o caso "digitou o código logado na conta errada"). Ponteiros corrigidos em `src/hooks/useEntitlement.ts` (dizia só "SQL Editor"), `src/lib/trip-admin-code.ts` (a alegação de substituição virou o recorte real), e ponteiros novos de volta para o runbook em `src/lib/trip-admin-code.functions.ts` e `src/lib/admin-login.functions.ts`.
- **Teste (1 novo, 524 → 525 — contagem medida contra a baseline da `main`, com as mudanças no stash, não estimada)**: `premium-entitlement.manual.server.test.ts` guardava "não rebaixa quem já é premium" só para `origem = 'stripe'`; o runbook afirma isso para **qualquer** origem, então o caso `pacote_visto` — o que importa na operação — passou a ter teste próprio. Suíte inteira verde (525), `lint` no baseline da `main` (0 erros, 10 warnings pré-existentes), `prettier` limpo nos arquivos tocados.
- **Ressalva de verificação honesta**: `typecheck` falha neste ambiente com `Cannot find module '@lovable.dev/cloud-auth-js'` em `src/integrations/lovable/index.ts` — arquivo que esta sessão não tocou. Confirmado como **pré-existente e ambiental**, não regressão: o tarball do pacote não baixa aqui (`ConnectionClosed`, 3 tentativas com backoff, via bun e npm), e rodar `typecheck` com as mudanças **guardadas no stash** produz exatamente o mesmo erro. O CI, com o pacote disponível, não deve reproduzir.
- **Pendência para o humano (inalterada, agora registrada na Seção 0)**: `TRIP_ADMIN_CODE` e `TRIP_ADMIN_EMAIL` continuam **não cadastradas**. Enquanto for assim, o Caminho A do runbook está desligado em produção e toda ativação fora do checkout depende do Caminho B.

### 2026-07-25 — VJT-011d Login próprio do Trip + acesso da equipe por código — **implementado, aguardando envs e Redirect URLs** (mesma branch/PR do VJT-011c)
- **Motivação**: `/trip/*` redirecionava para `/portal/login`, tela desenhada para cliente da consultoria de vistos (fala em "sua consultora", pede código de 6 dígitos gerado por ela, e o magic link roda com `shouldCreateUser: false`). Para o Trip vendido separado isso é um beco sem saída: o comprador não tem código, não tem consultora e a conta nem existe. Foi exatamente o que travou o dono do produto ao tentar entrar para usar o código de acesso do VJT-011c.
- **Decisão de arquitetura (do dono do produto)**: **banco centralizado** — um único projeto Supabase para os dois produtos, com `auth.users` compartilhado. O que se aparta é a **experiência de entrada**, não a base. Consequência boa: quem já é cliente Viajaly de visto entra no Trip com o mesmo e-mail e cai na mesma conta, sem cadastro duplicado e sem migração; e uma sessão ativa no portal simplesmente passa pelo gate de `/trip/*`. É por isso que **não existe fluxo de SSO próprio** — não há dois sistemas de identidade para federar; o "entrar como cliente Viajaly" é literalmente usar o mesmo e-mail.
- **`/trip/login`** (`src/routes/trip_.login.tsx`): o underscore no nome do arquivo é obrigatório — mantém a URL `/trip/login` mas sai do layout `trip.tsx`, que exige sessão. Sem isso a tela de login cairia no próprio gate e entraria em loop de redirect. `next` é validado como caminho interno (`startsWith("/")`), então não vira redirect para domínio externo. Com sessão ativa, a rota manda direto para `next`.
- **Três caminhos de entrada, nessa ordem**:
  1. **Google Sign-In** (principal) — provider habilitado no painel pelo dono do produto durante a execução deste ticket. Um toque, sem depender de entrega de e-mail.
  2. **Link mágico por e-mail** com `shouldCreateUser: true` — a diferença que torna o Trip vendável sozinho: cria a conta na hora, sem senha.
  3. **Código da equipe** (mesmo `TRIP_ADMIN_CODE` do VJT-011c), escondido atrás de "Acesso da equipe": entra sem esperar e-mail nenhum, para QA, demo e suporte.
- **Segurança do login por código**: o código **não autentica um e-mail digitado na tela** — abre exclusivamente a conta de `TRIP_ADMIN_EMAIL`. É credencial compartilhada de uma conta específica, não chave mestra capaz de entrar na conta de qualquer cliente; a distinção é o que limita o estrago se o código vazar. A sessão sai do mesmo mecanismo já usado no portal (`admin.generateLink` + `verifyOtp` no client), então o produto não ganha um segundo caminho de autenticação. Rate-limit por IP (mesmo módulo do VJT-011c, mesma ressalva de memória de processo). Se a conta da equipe ainda não existir no ambiente, é criada já confirmada em vez de exigir cadastro manual no painel. Cada entrada grava `trip_admin_login` em `audit_log`.
- **O mesmo código entra e já libera o Premium**: quem entra por ele é equipe testando o produto inteiro, e exigir digitar o código de novo em `/trip/mais` seria só uma etapa a mais. Best-effort — falha na ativação não derruba um login já autorizado.
- **Arquivos novos**: `src/routes/trip_.login.tsx`, `src/components/trip/auth/TripLogin.tsx` + `.test.tsx`, `src/lib/admin-login.functions.ts`. Editados: `src/routes/trip.tsx` (redirect agora para `/trip/login`), `src/components/trip/mais/DeleteAccountCard.tsx` (+teste; pós-exclusão volta para o login do Trip, não para o do portal), `src/lib/trip-admin-code.ts` (`parseAdminEmail`), `src/routeTree.gen.ts` (regenerado).
- **Não tocado de propósito**: `/portal/login` continua exatamente como está — é a porta do produto de vistos, com o fluxo de código de 6 dígitos da consultoria. `whatsapp.ts` e `/vistos` seguem apontando para lá.
- **Testes (8 novos, 495 → 503)**: `next` preservado no redirect do Google · link por e-mail normalizando o endereço e criando conta · e-mail inválido barrado antes do Supabase · campo da equipe escondido até ser pedido · código válido abrindo sessão com o token do servidor · código recusado sem abrir sessão · `parseAdminEmail` (2). `typecheck` e `build` limpos; `lint` no baseline da `main`.
- **Pendências para o humano**: (1) `TRIP_ADMIN_EMAIL` no ambiente, junto de `TRIP_ADMIN_CODE` — sem os dois, o acesso da equipe fica desligado e a tela responde "Acesso da equipe não está configurado neste ambiente"; (2) conferir no painel do Supabase (Auth → URL Configuration) que `https://viajaly.com/**` está nas Redirect URLs, senão o retorno do Google e do link mágico não volta para o app.

### 2026-07-25 — VJT-011c Código de acesso admin — **implementado, aguardando configuração da env em produção** (branch `claude/travel-app-access-link-t4ig4u`)
- **Motivação**: o runbook do VJT-011b resolve a ativação manual, mas exige SQL Editor + achar o `user_id` — inviável para o uso diário (QA, demo, bônus da consultoria). O código de acesso cobre exatamente esse caminho; o checkout Stripe (VJT-012) continua sendo a via de receita, intocado.
- **Como funciona**: o usuário digita o código em `/trip/mais` → server fn autenticada confere contra `TRIP_ADMIN_CODE` → `entitlements` recebe `plano='premium', origem='manual', expires_at=null` para o **próprio** usuário da sessão. O Realtime do VJT-011 já propaga, e a mutação ainda invalida a query explicitamente (backstop se o canal estiver caído). Nenhuma tela nova: o Premium liberado é o mesmo dos outros caminhos.
- **Decisões de segurança**:
  - Código **só em variável de ambiente do servidor** (`TRIP_ADMIN_CODE`, sem `VITE_`), nunca em arquivo versionado e nunca no bundle do client. Aceita vários códigos separados por vírgula para rotacionar sem janela de indisponibilidade.
  - Código com menos de 12 caracteres normalizados é **ignorado** — configuração fraca deixa o recurso desligado em vez de existir um código adivinhável liberando Premium vitalício.
  - Comparação em tempo constante e sem short-circuit na lista, para o tempo de resposta não vazar prefixo correto nem posição do código que casou.
  - Bloqueio de 15 min após 5 tentativas erradas, por usuário. **Ressalva registrada**: o contador vive na memória do processo — em serverless cada instância tem o seu, então é atrito contra força bruta ingênua, não garantia distribuída. O controle forte é a entropia do código. Se o volume de tentativas virar problema real, o passo seguinte é tabela no Postgres, não mais estado em memória.
  - A ativação é **sempre para o usuário autenticado** — não existe parâmetro de `user_id` na server fn, então um código vazado libera Premium para quem o tem, mas nunca vira ferramenta de escrita em conta alheia.
  - Cada resgate grava `trip_admin_code_redeemed` em `audit_log` (service role, best-effort: falha de log não derruba uma ativação que já aconteceu).
- **Nunca rebaixa quem já é Premium**: `activateManualPremiumEntitlement` lê a linha antes e, se ela já resolve para premium (`stripe`/`pacote_visto`/`manual`), retorna sem escrever — reescrever trocaria `origem='stripe'` de um pagamento real por `'manual'` e apagaria o rastro da compra. Para premium **expirado**, reativa e preserva `stripe_payment_id` (o upsert só manda as colunas da ativação; PostgREST não mexe no resto), então o índice único do VJT-012 continua válido.
- **Arquivos novos**: `src/lib/trip-admin-code.ts` (**puro**, mesma convenção de `entitlements.ts`: normalização, leitura da env, comparação, bloqueio) + `.test.ts`, `src/lib/trip-admin-code.functions.ts` (server fn), `src/lib/premium-entitlement.manual.server.test.ts`, `src/hooks/useRedeemAdminCode.ts` + `.test.tsx`, `src/components/trip/mais/AdminCodeCard.tsx` + `.test.tsx`. Editados: `src/lib/premium-entitlement.server.ts` (nova função, o caminho do webhook Stripe não muda), `src/components/trip/mais/MaisDashboard.tsx` (card nas duas variantes da tela — com e sem trip criada, porque o resgate não depende de já existir viagem).
- **Tabelas/RLS tocadas**: **nenhuma** — sem migration. `entitlements` continua só com `ent_select_own` e nenhuma policy de escrita para o client; toda escrita é service role no servidor, como no VJT-012. `scripts/test-rls.sh` não se aplica.
- **Testes (29 novos, baseline `main` 466 → 495)**: módulo puro (normalização, rotação, código curto descartado, prefixo correto rejeitado, ciclo completo de bloqueio/expiração) · ativação (usuário novo, já premium por Stripe sem sobrescrever, expirado preservando `stripe_payment_id`, idempotência do resgate repetido) · hook (invalidação no sucesso, `ok:false` virando erro sem invalidar) · card (invisível para Premium e durante carregamento, envio, Enter, "já era Premium", erro do servidor). `bun run typecheck` e `bun run build` limpos; `bun run lint` volta ao baseline da `main` (2190 erros pré-existentes de libs vendorizadas, nenhum nos arquivos deste ticket).
- **Pendência para o humano**: cadastrar `TRIP_ADMIN_CODE` no ambiente (Lovable/Vercel — mesma limitação de acesso a painel já registrada para `VITE_SENTRY_DSN` e `VITE_POSTHOG_KEY`). **Sem a variável o recurso fica desligado** e o card responde "Ativação por código está desativada neste ambiente" — estado seguro por padrão. O valor do código foi entregue ao dono do produto fora do repositório e não aparece em nenhum arquivo versionado.

### 2026-07-25 — Aplicação das 7 migrations pendentes no Supabase (VJT-003 → VJT-017) — **Concluída e verificada em produção** (operação de infraestrutura, sem ticket próprio da Seção 7)
- **Ponto de partida, medido e não suposto**: a Seção 0 vinha registrando "Migration aplicada no Supabase: não verificável nesta sessão" desde o VJT-001. Esta sessão rodou primeiro um bloco de **pré-verificação somente leitura** no SQL Editor do Lovable Cloud — catálogo do Postgres (`information_schema`, `pg_policies`, `pg_proc`, `pg_constraint`, `pg_index`, `pg_publication_tables`), sem nenhum DDL — no formato `item | esperado | encontrado`, resiliente a tabela ausente via `to_regclass` + `query_to_xml`. Resultado: **somente o VJT-001 estava aplicado** (`checklist_templates`=15, `paises_visto`=14, 0 `stripe_payment_id` duplicados; todo o resto `AUSENTE`). O conector Supabase desta conta segue sem acesso a `urrlqljlibpzaqnemlwf` — reconfirmado nesta sessão via `list_projects`, que só devolve `crm-ai-studio`, `supabase-purple-book` e `the-loyalty`. A verificação foi feita por SQL executado no painel, não pelo conector.
- **Cruzamento Seção 0 × diretório, sem divergência**: de VJT-011 em diante o diretório tinha exatamente 5 arquivos e a Seção 0 listava exatamente esses 5 como pendentes; nenhum arquivo órfão, nenhuma entrada fantasma. Confirmado também o negativo que a Seção 0 afirmava (VJT-005/006/007/009/010/016 sem migration). O registro manual estava correto.
- **Duas divergências de descrição, corrigidas aqui**: (1) a Seção 0 descrevia a migration do VJT-013 só como o fix de bootstrap de RLS, mas o arquivo tem **duas metades** — o commit `3280fb5` acrescentou `trip_owner_id()` + `with check` no `trips_update`, fechando o buraco em que qualquer editor podia rodar `update trips set owner_id = <si mesmo>`. Aplicar só a primeira metade **criaria** a escalada de privilégio, já que é ela que torna `owner_id` load-bearing para leitura; (2) a descrição do VJT-014 omitia o índice `idx_ai_messages_created_at_user`.
- **Achado operacional que vale para o futuro**: três migrations desta onda compartilham o timestamp `20260725200000` (vjt012, vjt013, vjt017), porque nasceram em sessões paralelas. Com aplicação manual isso é inofensivo — e as três são mutuamente independentes, então a ordem entre elas não importa — mas colide na chave de versão se algum dia o projeto passar a usar `supabase db push`. Renomear duas delas antes disso.
- **Como foi aplicado**: um **único script consolidado**, montado por concatenação dos arquivos reais de `supabase/migrations/` (nada redigitado; cada trecho do resultado conferido byte a byte com `diff` contra o arquivo de origem, 7/7 idênticos), na ordem VJT-003 → 003b → 011 → 012 → 013 → 014 → 017, com marcadores `>>> [n/7]` para localizar falha. Tudo dentro de um único `BEGIN; ... COMMIT;` — exigência real do VJT-017, que tem `drop constraint` sem `IF EXISTS` e `create table`/`create policy` sem guarda, e não pode deixar metade aplicada. Acrescentada uma **guarda de pré-condição** (não pertence a nenhuma migration) que aborta a transação se `checklist_templates` não for 15, se `num_criancas` já existir ou se as tabelas do VJT-017 já existirem — proteção contra reexecução acidental, já que os catálogos são `INSERT` puro sem `ON CONFLICT` e duplicariam em silêncio.
- **Resultado**: aplicado com sucesso; a pós-verificação (mesmo bloco da pré) devolveu **27/27 linhas com `encontrado = esperado`** e `APLICADA` nos 8 vereditos. `checklist_templates` foi de 15 para **142**, batendo a assinatura documentada (15 → 97 com o VJT-003, → 142 com o VJT-003b).
- **O que isso destravou**: criar viagem estava quebrado em produção por dois motivos empilhados — `src/hooks/useCreateTrip.ts:80` insere `num_criancas` numa coluna que não existia, e, mesmo depois disso, o `.select("id")` do `INSERT ... RETURNING` reaplicava `trips_select`, que só permitia `is_trip_member(id)` e barrava o próprio criador antes do bootstrap de `trip_members`. VJT-003 e VJT-013 juntos resolvem os dois; nenhum dos dois sozinho resolve.
- **Pendência única que fica**: `20260725220000_vjt017b_lgpd_consent_history.sql`. O PR [#67](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/67) mergeou em 2026-07-25T19:57:33Z (commit `2714fa7`, confirmado via API do GitHub), então o arquivo já está na `main` — **pendente de execução, não de merge**. Seu pré-requisito (VJT-017 aplicado, quem cria `user_lgpd_consents`) passou a estar satisfeito nesta sessão. Enquanto não for aplicada, o deploy da `ai-chat` segue bloqueado: o gate versionado do VJT-017b não tem onde gravar histórico com a tabela ainda em `user_id` como PRIMARY KEY.
- **Escopo não tocado**: nenhuma alteração em código de aplicação, migration ou teste — esta sessão só leu o repositório, aplicou SQL já existente no banco e reescreveu as Seções 0 e 8.
### 2026-07-25 — VJT-015 Instrumentação PostHog — **PR aberto (draft), aguardando review** (issue [#66](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/66), PR [#68](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/68), branch `claude/posthog-vjt-015-mj9gmj`)
- **Pré-requisitos confirmados antes de começar**: VJT-011 mergeado (PR #51, `merged_at: 2026-07-25T15:39:36Z`). Busca por issue/PR com "VJT-015": zero resultados (`search_pull_requests` com `in:title`, e varredura das 23 issues do repositório) — a issue #66 é desta sessão. Como o ticket toca eventos espalhados por quase todo o app, também foi confirmado via API que os **únicos** PRs abertos eram #63 (troca do provider de IA para OpenRouter/DeepSeek, sem instrumentação) e #64 (fixture do teste de RLS, follow-up do VJT-013) — nenhum ticket de feature concorrente.
- **O que foi feito**: PostHog integrado ao client e os 12 eventos do PRD §2 disparando, mais os 2 funis. Nenhuma mudança de comportamento visível: o ticket só observa.
  - `src/lib/trip-analytics.ts` (novo, **puro**, mesma convenção de `trip-math.ts`/`entitlements.ts`): os 12 nomes de evento, o tipo de propriedade de cada um (`TripEventPayload<N>` — propriedade errada vira erro de typecheck, não métrica silenciosamente torta), a definição dos 2 funis como sequência ordenada, e `analyticsConfigFromEnv`. Nenhum nome de evento é escrito como string literal fora deste arquivo.
  - `src/lib/posthog.ts` (novo, adaptador): único arquivo que toca `posthog-js`, via `import()` **dinâmico** — com analytics desligado o navegador nem baixa a biblioteca (confirmado no build: `posthog-js` sai em chunk próprio de 222 kB, e o bundle de entrada só carrega a string do host default). `autocapture`/`capture_pageview`/session replay desligados: o escopo são os 12 eventos explícitos, e o app tem áreas fora do produto Trip (`/portal/*`, `/console/*`). `person_profiles: "identified_only"`.
  - **Reaproveitou os pontos que já existiam, sem duplicar disparo**: `visa_cta_click` ganhou a perna PostHog **dentro do próprio `trackVisaCtaClick`** (VJT-009), não um segundo `onClick` no card — dataLayer/gtag e PostHog veem o mesmo clique, uma vez cada (com teste de regressão do caminho antigo). `upgrade_view` vive em `openPaywall()` no `PaywallProvider` (VJT-011): um ponto só para os 6 gatilhos, com o gatilho como propriedade, em vez de 6 disparos espalhados. `upgrade_purchase` vive na tela de retorno do checkout (VJT-012), lendo o mesmo `useEntitlement()` — sem segundo mecanismo de leitura de plano.
  - **`signup`**: o produto não tem cadastro próprio (conta compartilhada com o app de visto via OTP), então o evento fica no consentimento LGPD (`useAcceptLgpdConsent`, VJT-017) — primeiro contato com o produto, como o próprio comentário do `LgpdConsentGate` já anotava. **Condicionado ao primeiro aceite do usuário**, não ao aceite: ver a nota de conflito com o VJT-017b logo abaixo.
  - Demais eventos no `onSuccess` da mutação correspondente (`useCreateTrip`, `useToggleChecklistItem`, `useAddSavingsEntry`, `useAddBudgetItem`, `useAiChat`, `useCreateTripInvite`, `useAcceptTripInvite`) e em `ItineraryBoard.handleExportPdf` depois do PDF gerado.
  - `useAnalyticsIdentity()` (novo) no layout `/trip/*` amarra os eventos ao `auth.users.id` (funil atravessa device/sessão) e faz `reset()` no logout. Só o produto Trip é identificado.
- **Conflito com o VJT-017b, resolvido antes do fim do ticket**: a spec original do VJT-015 ancorava `signup` no aceite do consentimento LGPD justificando que "a linha é única por usuário" — premissa do VJT-017. O VJT-017b (em paralelo) transforma `user_lgpd_consents` em log append-only, com `unique(user_id, versao_termos)`: cada bump de versão de termo faz a base inteira re-aceitar, e `signup` por aceite mandaria todo mundo de volta ao topo do funil grátis→compra a cada atualização de termos. **Caminho escolhido**: manter a âncora, condicionando o disparo ao PRIMEIRO aceite do usuário — `jaConsentiuAntes(userId)` conta linhas em `user_lgpd_consents` **antes** do insert, ignorando a versão. Preferido a mudar de âncora porque o consentimento continua sendo o único ponto que existe em 100% dos usuários do Trip e acontece antes de qualquer outra ação (não há cadastro próprio; `trip_created` já é o segundo passo do funil, e usar `entitlements` só cobriria quem paga). Falha da consulta de contagem = **não dispara**: perder um `signup` custa um usuário no topo do funil, disparar a mais corrompe a métrica de toda a base. Três testes cobrem isso, incluindo o re-aceite por bump de versão.
- **Decisões que mudam o número no painel** (as três mais fáceis de errar): `checklist_item_done` **não** dispara ao desmarcar (mede progresso, não interação, senão correção do usuário infla o total); `pdf_exported` só depois do arquivo gerado (clique de free vira `upgrade_view`, e falha na geração não conta); `upgrade_purchase` é deduplicado pelo `session_id` do Stripe em `sessionStorage`, então recarregar `/trip/checkout?session_id=…` não vira segunda compra.
- **Privacidade**: nenhum PII no payload — só uuid, enum, contador e centavos. Título de item, nota, nome de item de orçamento e a mensagem enviada à IA nunca saem do app (dois testes asseguram isso explicitamente). Analytics também nunca quebra a UX: toda captura é try/catch, exceção do PostHog não propaga para o `onSuccess` que a chamou.
- **Kill switch / rollout**: `VITE_POSTHOG_KEY` ausente = produto inteiro sem analytics (opt-in, igual ao Sentry — é o estado atual de dev, preview e produção); `VITE_POSTHOG_HOST` opcional; `VITE_ANALYTICS_ENABLED="false"` desliga a captura com a chave presente, sem deploy de código.
- **Arquivos novos**: `src/lib/trip-analytics.ts`+`.test.ts`, `src/lib/posthog.ts`+`.test.ts`, `src/hooks/useAnalyticsIdentity.ts`, `src/hooks/trip-analytics-events.test.tsx`, `src/components/trip/roteiro/ItineraryBoard.analytics.test.tsx`, `src/components/trip/paywall/PremiumCheckout.analytics.test.tsx`. Editados (uma chamada de `capture` cada, fora `trip-visa.ts`): `router.tsx`, `routes/trip.tsx`, `lib/trip-visa.ts`(+`.test.ts`), `hooks/useLgpdConsent.ts`, `useCreateTrip.ts`, `useTripChecklists.ts`, `useTripSavings.ts`, `useTripBudget.ts`, `useAiChat.ts`, `useTripMembers.ts`, `usePaywall.tsx`, `components/trip/roteiro/ItineraryBoard.tsx`, `components/trip/paywall/PremiumCheckout.tsx`, `package.json`/`bun.lock` (`posthog-js`).
- **Tabelas/RLS tocadas**: **nenhuma** — sem migration, sem policy, sem tabela nova. `scripts/test-rls.sh` não se aplica (regra de processo do VJT-013 vale para ticket que toca RLS; este não toca).
- **Testes (56 novos, baseline `main` 372 → 428)**: contrato dos 12 eventos e dos 2 funis (13) · adaptador: config de ambiente, kill switch, fila, dedup por `sessionStorage`, exceção não vazando (13) · `trackVisaCtaClick` com regressão do dataLayer/gtag do VJT-009 (3) · pontos de disparo em hooks, incluindo os "não dispara" — re-aceite de termo, desmarcar item, cota de IA esgotada, criação de trip que falha (20) · `pdf_exported` nos 3 desfechos do botão (3) · `upgrade_purchase` com dedup e origem real (4).
- **QA manual real (Playwright, Chromium, dev server)**, 3 cenários: (1) com o `.env` atual, sem chave — `/trip` redireciona para `/portal/login?next=%2Ftrip` como antes, `capture()` não lança, e **zero** requisições para o PostHog; (2) com chave injetada — 3 requisições reais a `us.i.posthog.com`/`us-assets.i.posthog.com`, provando que a integração de fato chega ao destino (o teste unitário mocka a biblioteca, então esse caminho só se verifica no browser); (3) chave presente + `VITE_ANALYTICS_ENABLED="false"` — zero requisições. Nenhum erro de console atribuível ao ticket (comparado contra a `main` no mesmo dev server).
- **Verificação local**: bun 1.3.11 — lint 0 erros (10 warnings, idênticas às da `main`, verificado por `git stash`), typecheck 0 erros, test 425/425, build ok. `bun run build` regenera 4 rotas de MCP (`src/routes/[.mcp]/*`, `mcp.ts`, `[.well-known]/*`) por efeito do plugin `@lovable.dev/mcp-js` — revertidas antes do commit, mesmo cuidado com reformatação incidental fora de escopo já adotado no VJT-004b/VJT-011/VJT-012.
- **Como testar no celular**: com `VITE_POSTHOG_KEY` cadastrada no Vercel, abrir `/trip` → aceitar o consentimento LGPD (`signup`) → criar a viagem (`trip_created`) → marcar um item de checklist (`checklist_item_done`) → registrar economia (`savings_entry_created`) → adicionar item de orçamento (`budget_item_created`) → em destino que exige visto, tocar "Falar com a consultoria" (`visa_cta_click`) → tocar "Exportar PDF" como free (`upgrade_view`) → assinar (`upgrade_purchase`) → exportar o PDF já premium (`pdf_exported`) → convidar alguém (`invite_sent`) e aceitar na outra conta (`invite_accepted`) → mandar uma mensagem para o assistente (`ai_message_sent`). Os 12 devem aparecer no Live Events do PostHog, um por ação.
- **Pendências deixadas para tickets futuros**: cadastro de `VITE_POSTHOG_KEY` no Vercel e criação dos 2 funis no painel do PostHog (ação manual do humano, mesma limitação do `VITE_SENTRY_DSN`); evento `nps_submitted` do VJT-017 continua fora — não está entre os 12 do PRD §2 e não foi adicionado por conta própria.

### 2026-07-25 — VJT-012 Checkout Stripe + webhook de ativação do Premium — **Mergeado** (issue [#54](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/54), PR [#55](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/55), mergeado em 2026-07-25T17:22:20Z, confirmado via API do GitHub — `merged: true`, `merged_by: mzinhoww-svg`, commit de merge `dc27c5f`)
- **Pré-requisito confirmado antes de começar**: VJT-011 estava "executável agora" segundo a Seção 0, mas ainda como PR #51 aberto/draft — a sessão parou e aguardou o merge humano antes de iniciar qualquer trabalho, conforme pedido explicitamente. VJT-011 mergeou em 2026-07-25T15:39:36Z (commit `d0f542d`) durante a espera; confirmado via API do GitHub (`merged: true`) e via `git log origin/main` antes de prosseguir. Zero issue/PR existente com "VJT-012" (`search_issues`/`search_pull_requests`) — só a issue #54 criada por esta sessão.
- **O que foi feito**: checkout Stripe one-time do Premium, reaproveitando 100% da infraestrutura já existente no repositório para consultoria/taxas (`src/lib/stripe.server.ts`, `src/lib/payments-stripe.functions.ts`, `src/routes/api/public/payments/webhook.ts`) — **não** criou uma função Supabase Edge nova; o app já processa Stripe via server functions do TanStack Start + uma rota de API própria, então o VJT-012 estende esse mesmo caminho em vez de inventar um segundo.
  - `createPremiumCheckout` (novo, em `payments-stripe.functions.ts`): sessão `mode: "payment"`/`ui_mode: "embedded_page"`, `metadata: { user_id, kind: "premium" }` (o `user_id` vem do JWT autenticado via `requireSupabaseAuth`, nunca de entrada do client). Kill switch `PAYMENTS_ENABLED` (novo, `stripe.server.ts`) falha fechado — variável ausente ou diferente de `"true"` bloqueia a criação do checkout; short-circuit adicional se o usuário já é premium (reaproveita `resolveEntitlement`, sem duplicar a regra).
  - `src/lib/premium-entitlement.server.ts` (novo): `activatePremiumEntitlement(userId, paymentId)` — upsert em `entitlements` (`plano='premium'`, `origem='stripe'`, `expires_at=null`) via service role, **idempotente por `stripe_payment_id`**: se a linha já tem esse mesmo payment_id, não reescreve (cobre tanto reenvio do mesmo evento do Stripe quanto os dois eventos distintos que o Pix gera para o mesmo pagamento). `readPremiumUserId` valida o formato uuid da metadata antes de usar.
  - `webhook.ts`: `CheckoutKind` ganha `"premium"`, dispatchado em `handleCheckoutCompleted` (sessão) e `handlePaymentIntentSucceeded` (Pix assíncrono) — mesmo padrão já usado para `"taxes"`/`"consultancy"`, nenhuma duplicação de lógica de assinatura/dedup por evento (`stripe_webhook_events`, já existente, continua sendo a primeira camada de dedup).
  - Migration (`20260725200000_vjt012_premium_checkout_idempotency.sql`): índice único parcial em `entitlements.stripe_payment_id` (`WHERE stripe_payment_id IS NOT NULL`) — segunda camada de defesa da idempotência, a nível de banco.
  - **Sem tocar o design do `PaywallModal`** (VJT-011): só o `Button` antes desabilitado ("em breve") virou um `onClick` que fecha o modal e navega para `/trip/checkout` (rota nova, dedicada); se `!paymentsConfigured()`, o botão continua desabilitado ("Pagamentos não configurados") em vez de quebrar.
  - `src/components/trip/paywall/PremiumCheckout.tsx` + rota `trip.checkout.tsx` (novos): página com abas Pix/Cartão (`EmbeddedCheckoutProvider`/`EmbeddedCheckout`, mesmo componente já usado em `TaxList.tsx`/`portal.pagamento.tsx`). **Sem polling próprio**: lê `useEntitlement()` (VJT-011) — quando `isPremium` vira `true` (via a assinatura `postgres_changes` já existente), mostra a tela de sucesso; nenhum segundo mecanismo de leitura de plano foi criado, conforme pedido.
- **Segurança**: `activatePremiumEntitlement` não é uma RPC `SECURITY DEFINER` chamável pelo client — é uma função server-only que só o webhook (service role) invoca, evitando o risco de um `SECURITY DEFINER` com `user_id` como parâmetro livre ser explorável por qualquer usuário autenticado (padrão diferente de `mark_paid_from_stripe`, que se apoia só na obscuridade do `session_id`; aqui optei por uma segunda camada — nenhuma superfície SQL nova exposta ao client). `entitlements` continua só com `ent_select_own` — nenhuma policy de escrita nova.
- **Kill switch**: `PAYMENTS_ENABLED` só bloqueia a criação de checkout novo — um pagamento já coletado pelo Stripe é sempre ativado pelo webhook, mesmo que a flag seja desligada depois da sessão ser criada (decisão deliberada: não faria sentido cobrar e não entregar).
- **Arquivos novos**: `src/lib/premium-entitlement.server.ts`+`.test.ts`, `src/lib/stripe.server.test.ts`, `src/components/trip/paywall/PremiumCheckout.tsx`, `src/routes/trip.checkout.tsx`, `supabase/migrations/20260725200000_vjt012_premium_checkout_idempotency.sql`. Editados: `src/lib/entitlements.ts` (+`PREMIUM_PRICE_BRL_CENTS`), `src/lib/stripe.server.ts` (+`paymentsEnabled`), `src/lib/payments-stripe.functions.ts` (+`createPremiumCheckout`), `src/routes/api/public/payments/webhook.ts` (+dispatch do kind `"premium"`), `src/components/trip/paywall/PaywallModal.tsx` (só o CTA), `src/components/trip/wizard/CreateTripGate.test.tsx` (mock de `@tanstack/react-router` precisou de `useNavigate` depois da mudança no `PaywallModal`).
- **Tabelas/RLS tocadas**: só o índice único em `entitlements.stripe_payment_id` (sem mudança de policy) — `entitlements` continua com apenas `ent_select_own`.
- **Testes (14 novos, baseline `main` 274 → 288)**: `entitlements.test.ts` +1 (`PREMIUM_PRICE_BRL_CENTS`) · `stripe.server.test.ts` 8 novos (`paymentsEnabled` falha fechado sem variável/valor errado/só `"true"` libera; `verifyWebhook` aceita assinatura válida, rejeita segredo errado, corpo adulterado, cabeçalho ausente, timestamp fora da janela — cobre o cenário "assinatura inválida" pedido para este ticket) · `premium-entitlement.server.test.ts` 5 novos (`readPremiumUserId` válido/inválido; `activatePremiumEntitlement`: sessão completada válida ativa; evento duplicado com mesmo `stripe_payment_id` não reescreve, `upsert` chamado só 1x; pagamento de usuário já premium por outra origem aplica sem duplicar a linha, chave é `user_id`). Os 4 cenários pedidos no ticket (sessão completada válida, evento duplicado, assinatura inválida, pagamento de usuário já premium) estão cobertos nesses dois arquivos — não há teste de rota (`webhook.ts` em si) porque nenhum checkout Stripe existente no repositório (consultancy/taxes) tem esse nível de teste hoje; a superfície nova foi extraída para módulos isolados e testáveis (`premium-entitlement.server.ts`) em vez de inventar um harness de mock de rota que não tem precedente no projeto.
- **Verificação local**: bun 1.3.11 — lint 0 erros (revertidas reformatações incidentais fora de escopo de `bun run format`, que tocou vários arquivos não relacionados ao ticket — mesmo padrão de cautela do VJT-004b/VJT-011), typecheck 0 erros, test 288/288 (14 novos), build ok (`routeTree.gen.ts` regenerado para incluir `/trip/checkout`).
- **Pendências deixadas para tickets futuros**: aplicação manual da migration deste ticket em produção (sem ela, o índice único não existe ainda, mas o upsert por `user_id` continua funcionando normalmente); configuração real de `PAYMENTS_ENABLED`/`STRIPE_*_API_KEY`/`PAYMENTS_*_WEBHOOK_SECRET` no ambiente de produção (fora do alcance desta sessão, mesma limitação já registrada para `VITE_SENTRY_DSN`); validação end-to-end com o Stripe CLI/sandbox real (não disponível nesta sessão — só testes unitários dos módulos novos).
- **Como testar no celular**: com `PAYMENTS_ENABLED=true` e chaves Stripe sandbox configuradas, `/trip/mais` → card "Seu plano" free → qualquer um dos 6 gatilhos → modal → "Assinar Premium" → `/trip/checkout` → aba Pix ou Cartão → completar pagamento de teste → volta para `/trip/checkout?session_id=...` → tela "Confirmando seu pagamento…" até o webhook processar → tela de sucesso "Você já é Premium", sem reload da página.
- **Follow-up de segurança nesta mesma sessão (antes de qualquer merge)**: revisão própria identificou que `verifyWebhook` (`src/lib/stripe.server.ts`, código pré-existente reaproveitado por este ticket) comparava a assinatura HMAC via `v1Signatures.includes(expected)` — comparação de string comum, que retorna assim que encontra o primeiro byte divergente, abrindo uma janela teórica de timing attack para inferir a assinatura correta byte a byte. Substituído por `timingSafeEqualHex` (XOR acumulado, sempre percorre a string inteira) + teste dedicado que espiona `String.prototype.charCodeAt` e confirma que o número de chamadas é idêntico para divergência na posição 0 e na última posição (290/290 testes, 2 novos).
- **Merge conflito durante o babysitting do PR**: `main` avançou com VJT-016 (PR #53) e VJT-014 (PR #61) enquanto o PR #55 estava aberto, deixando-o `dirty`. Resolvido dando merge de `main` de volta na branch (commit `84ea20c`) — só `VIAJALY-TRIP.md` conflitou (Seção 0/Log editada por sessões paralelas ao mesmo tempo); nenhum arquivo de código. Lint/typecheck/test (313/313 depois do merge)/build reverificados antes de repush. Nessa reconciliação, sinalizei que VJT-014 estava mergeado sem entrada de log — pendência que esta mesma entrada de consolidação de onda (abaixo) fecha.
- **Merge**: PR #55 mergeado por `mzinhoww-svg` (`merged_by`), commit de merge `dc27c5f`, confirmado via API do GitHub (`merged: true`, `merged_at: 2026-07-25T17:22:20Z`), não assumido. CI (`ci` check) `completed`/`success` no commit final. Enquanto este PR estava aberto, `main` também recebeu VJT-013 (PR #59, 17:21:55Z) e VJT-017 (PR #57, 17:22:06Z) de sessões paralelas — as três entradas de log ficam registradas logo abaixo, escritas a partir do corpo real de cada PR (via API do GitHub) já que esta sessão não implementou esses tickets.

### 2026-07-25 — VJT-017 LGPD + modo concluída + NPS — **Mergeado** (PR [#57](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/57), issue [#56](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/56), mergeado em 2026-07-25T17:22:06Z, confirmado via API do GitHub — `merged: true`, `merged_by: mzinhoww-svg`)
> Entrada escrita por uma sessão diferente (VJT-012) a partir do corpo real do PR #57, para fechar a divergência entre `main` (já mergeado) e este Log — não é relato em primeira pessoa de quem implementou.
- **O que foi feito**: consentimento LGPD via `LgpdConsentGate` (novo, monta em torno de todo `trip.tsx`, bloqueia até aceite explícito, grava em `user_lgpd_consents` nova, RLS restrita à própria linha) — o produto não tem signup próprio (conta compartilhada com o app de visto via OTP), então o primeiro acesso a `/trip/*` é o ponto de coleta de consentimento. Exclusão de conta via Edge Function `delete-account` (valida token do chamador, usa `admin.auth.admin.deleteUser` com service role) + `DeleteAccountCard` em `/trip/mais` (exige digitar "EXCLUIR"). Modo concluída ganha `TripNpsForm` (nota 0-10 + comentário, upsert por `(trip_id, user_id)`, não duplica em reenvio) dentro do `ConcludedSummary` já existente (VJT-004) — CTA "Planejar nova viagem" inalterado, já passava pelo gate do VJT-011.
- **Decisão de design relevante**: a exclusão de conta expunha uma tensão real entre LGPD Art. 18 (direito à eliminação) e Art. 12 (dado anonimizado deixa de ser pessoal) em `savings_entries` — cascatear a linha inteira ao excluir um membro não-owner apagaria um valor que `calcularAcumulado` soma para **todos** os membros da trip, afetando dado coletivo de terceiros. Resolvido trocando a FK de `savings_entries.created_by` de `CASCADE` para `SET NULL` (o valor sobrevive, só perde a atribuição); `ai_conversations.created_by` e `trip_nps_responses.user_id` permanecem `CASCADE` (dado genuinamente pessoal, sem agregado compartilhado).
- **Migration** (`20260725200000_vjt017_lgpd_nps_account_deletion.sql`): FK fixes acima + `ai_conversations`/`trip_invites` corrigidas para `CASCADE` de verdade (fechando um bug do VJT-001 em que `deleteUser()` falhava por violação de FK quando o excluído não era o owner da trip) + cria `trip_nps_responses`/`user_lgpd_consents` (RLS restrita à própria linha/membro).
- **Testes (39 novos, baseline `main` 274 → 314 na branch)**: cobrindo consentimento (hook + gate), NPS (módulo puro + hook + form), exclusão de conta (hook + card), e um teste de contrato lendo o SQL real da migration para confirmar `SET NULL`/`CASCADE` nas colunas certas (mesma limitação de acesso a Postgres real já registrada no arquivo). QA manual real via Playwright, 11/11 checks.
- **Verificação local (relatada no PR)**: lint/typecheck 0 erros, test 314/314, build ok.
- **Pendências deixadas para tickets futuros**: aplicação manual da migration em produção; evento `nps_submitted` (VJT-015); runbook de exportação de dados LGPD (fora de escopo, não pedido na Seção 7).

### 2026-07-25 — VJT-013 Convites e membros — **Mergeado** (PR [#59](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/59), issue [#58](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/58), mergeado em 2026-07-25T17:21:55Z, confirmado via API do GitHub — `merged: true`, `merged_by: mzinhoww-svg`)
> Entrada escrita por uma sessão diferente (VJT-012) a partir do corpo real do PR #59, para fechar a divergência entre `main` (já mergeado) e este Log — não é relato em primeira pessoa de quem implementou.
- **O que foi feito**: fluxo real de convite sobre o schema que já existia desde o VJT-001 (`trip_invites`, `trip_members`, RPC `accept_trip_invite`) e o gate de plano do VJT-011 — nenhum schema de convite novo, nenhuma RPC recriada, nenhum modal de paywall novo. `InviteMemberCard` (o toast "em breve" do VJT-011) vira o fluxo real: cria convite, mostra link com copiar; free continua abrindo o mesmo `openPaywall("convidar_membro")`. `TripMembersList` novo (dono remove membro, membro sai, com confirmação). Rota `/trip/aceitar-convite?token=...` exige clique explícito (nunca automático no mount, já que o token é de uso único e um preload de e-mail/chat o queimaria sozinho). Kill switch `INVITES_ENABLED` (`VITE_INVITES_ENABLED="false"` desliga).
- **Bug de RLS pré-existente encontrado e corrigido**: o primeiro teste real de RLS deste projeto (infra nova, `scripts/test-rls.sh`, sobe um Postgres descartável com mock do schema `auth`) revelou que `trips_select` (só `is_trip_member(id)`) bloqueava o próprio dono de ver a trip recém-criada, antes do bootstrap de `trip_members` — quebra desde o VJT-001, nunca pega porque nenhum ticket anterior executava as policies de verdade. Corrigido em `20260725200000_vjt013_trips_select_owner_bootstrap.sql`: `trips_select` passa a aceitar `is_trip_member(id) or owner_id = auth.uid()`.
- **Testes (36 novos, baseline 257 → 293 na branch)** + `scripts/test-rls.sh` cobrindo 6 cenários de RLS contra Postgres real (não-membro vê zero linhas, não escreve, aceite de convite promove a `editor`, convite já aceito/expirado rejeitado). QA manual real via Playwright.
- **Verificação local (relatada no PR)**: lint/typecheck 0 erros, test 293/293, `test-rls.sh` PASS, build ok.
- **Pendências deixadas para tickets futuros**: aplicação manual da migration em produção (o bug de RLS já existe hoje, não é regressão deste PR); eventos `invite_sent`/`invite_accepted` (VJT-015).

### 2026-07-25 — VJT-014 Assistente IA — **Mergeado** (PR [#61](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/61), issue [#60](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/60), mergeado em 2026-07-25T17:05:31Z, confirmado via API do GitHub — `merged: true`, `merged_by: mzinhoww-svg`)
> Entrada escrita por uma sessão diferente (VJT-012) a partir do corpo real do PR #61, para fechar a divergência entre `main` (já mergeado) e este Log — não é relato em primeira pessoa de quem implementou.
- **O que foi feito**: Edge Function `supabase/functions/ai-chat` (Deno) chama a Anthropic no modelo `claude-haiku-4-5` (classe Haiku, Seção 3), chave lida só de `Deno.env`, nunca no client. Módulo puro novo `src/lib/ai-assistant.ts` (mesma convenção de `trip-math.ts`/`entitlements.ts`): validação de mensagem, detecção determinística de pergunta de visto por palavra-chave, bloco de contexto da trip, prompt de sistema. Contexto real da trip injetado no prompt via `determinarModoTrip`/`calcularMesesRestantes` (`trip-math.ts`, sem reimplementar fórmula). Cotas 10/100 vêm de `entitlements.ts`/`useEntitlement()` (VJT-011), sem lógica de plano paralela nem no client nem na Edge Function; cota esgotada abre o mesmo modal único (`openPaywall("cota_ia")`). Pergunta de visto responde direto com o link de WhatsApp+UTM do VJT-009 (`buildVisaConsultoriaLink`, reaproveitado) sem gastar chamada à Anthropic.
- **Kill switches/proteção de custo** (secrets de Edge Function): `AI_ENABLED` (default ativo, `"false"` → 503 em toda chamada) + `AI_DAILY_MSG_CAP` (default 300, teto **global** diário, distinto da cota por usuário — protege contra abuso/loop).
- **Migration** (`20260725210000_vjt014_ai_assistant.sql`): 2 funções `security definer`, nenhuma tabela/policy nova — `increment_ai_usage()` upsert atômico lendo `auth.uid()` internamente (nunca por parâmetro, então ninguém incrementa cota alheia); `ai_daily_message_count()` para o teto global (só devolve um número, nenhum dado de mensagem).
- **Segurança**: Edge Function usa client com o JWT do chamador (anon key + Authorization), nunca service role — isolamento entre trips vem da RLS já existente (`is_trip_member`).
- **Testes (16 novos, baseline `main` 274 → 290 na branch)**: validação de mensagem, detecção de visto (com/sem acento, falsos positivos), bloco de contexto nos 3 modos, prompt de sistema, chat (contexto real, redirecionamento de visto, recusa fora do tema, paywall em cota esgotada). QA manual real via Playwright cobrindo os 4 critérios de aceite, incluindo confirmar que a Edge Function não é chamada quando a cota já está esgotada.
- **Verificação local (relatada no PR)**: lint/typecheck 0 erros, test 290/290, build ok.
- **Pendências deixadas para tickets futuros**: aplicação manual da migration + cadastro de secrets em produção; evento `ai_message_sent` (VJT-015); histórico de chat ao reabrir o sheet (conversa persistida, mas UI abre em branco).

### 2026-07-25 — VJT-016 PWA — **Mergeado** (PR [#53](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/53), issue [#52](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/52), mergeado em 2026-07-25T16:40:54Z, confirmado via API do GitHub — `merged: true`, `merged_by: mzinhoww-svg`, commit de merge `670b918`)
- **Pré-requisito confirmado antes de despachar**: VJT-011 mergeado (`merged: true`, PR #51, `merged_at: 2026-07-25T15:39:36Z`), verificado via API do GitHub nesta sessão — a Seção 0 já registrava isso no momento em que a sessão começou. Busca por issue/PR "VJT-016" existente feita antes de abrir a issue: zero resultados (`search_issues`/`search_pull_requests`).
- **O que foi feito**: `/trip/*` agora instalável como sub-marca **Viajaly Trip**, sem tocar lógica de negócio (Stripe/convites/IA/LGPD fora de escopo, conforme fronteira do ticket).
  - `public/trip/manifest.webmanifest`: `name`/`short_name` "Viajaly Trip", `start_url`/`scope` = `/trip`, `display: standalone`, `theme_color`/`background_color` = `#10204A`, 3 ícones (192/512 `any` + 512 `maskable`).
  - `public/trip/icons/*.png`: gerados a partir do mesmo glifo do logo Viajaly (`src/components/viajaly/Logo.tsx`), recolorido (fundo navy + glifo coral) para diferenciar a sub-marca mantendo a família visual. Rasterizados via Chromium headless (`headless_shell`) a partir do SVG original, sem dependência nova no `package.json`.
  - `public/trip/splash/*.png`: `apple-touch-startup-image` para os 8 breakpoints de iPhone mais comuns em uso hoje (SE/8 até 16 Pro Max, retrato), cada um com media query exata de `device-width`/`device-height`/`-webkit-device-pixel-ratio`. Android usa o splash automático do browser a partir do manifest — sem asset extra necessário. iPad fora de escopo (produto mobile-first, base 375px).
  - `src/lib/trip-pwa.ts` (+`.test.ts`, 7 testes novos): módulo puro que descreve os links de `<head>` (manifest, apple-touch-icon, splash + media queries), seguindo a mesma convenção de módulo puro testável do resto do backlog.
  - `src/routes/trip.tsx`: `head()` novo no layout pathless `/trip` (mesmo mecanismo de merge de head já usado por `trip.roteiro.tsx`/`trip.checklists.tsx` para título) — só soma os links/meta tags acima ao head de `__root.tsx`; `/portal/*` (app principal) não é afetado.
- **Arquivos novos**: `public/trip/manifest.webmanifest`, `public/trip/icons/{icon-192,icon-512,icon-maskable-512,apple-touch-icon}.png`, `public/trip/splash/apple-splash-{750x1334,1242x2208,828x1792,1125x2436,1242x2688,1170x2532,1179x2556,1290x2796}.png`, `src/lib/trip-pwa.ts`+`.test.ts`. Editado: `src/routes/trip.tsx`.
- **Tabelas/RLS**: nenhuma — ticket é só assets estáticos e metadata de `<head>`, sem migration.
- **Verificação manual real (Playwright + Chromium headless, viewport 375×812, rede Supabase mockada)**: sem dispositivo físico disponível nesta sessão, então a verificação seguiu o padrão equivalente definido na própria issue (#52) — `Page.getAppManifest` via CDP (mesma API do DevTools → Application → Manifest) em `/trip` retornou zero erros, manifest parseado corretamente; `Page.getInstallabilityErrors` via CDP (base do audit do Lighthouse) retornou só `in-incognito` (artefato do contexto do Playwright, não um problema real); `/trip` inclui os links novos, `/portal/login` não inclui nenhum (sem regressão cruzada no head do portal principal); `fetch("/trip/manifest.webmanifest")` no browser real respondeu 200 com JSON válido.
- **Verificação local**: bun 1.3.11 — lint 0 erros (só warnings pré-existentes não relacionados), typecheck 0 erros, test 281/281 (7 novos), build ok — assets confirmados em `.output/public/trip/**`.
- **Como testar no celular**: `/trip` (logado) no Chrome Android → "Adicionar à tela inicial"/"Instalar app" com ícone navy/coral e nome "Viajaly Trip"; no Safari iOS → "Adicionar à Tela de Início" com o mesmo ícone, app abre em modo standalone com splash na cor da marca.
- **Pendências deixadas para tickets futuros**: instalação em dispositivo físico real (Android/iOS) ainda não feita — recomendado validar manualmente antes ou logo depois do merge; service worker de cache/offline avançado permanece fora de escopo (não pedido pela Seção 2/7 para este ticket).

### 2026-07-25 — VJT-011 Entitlements + paywall único — **Mergeado** (PR [#51](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/51), issue [#50](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/50), mergeado em 2026-07-25T15:39:36Z, confirmado via API do GitHub)
- **Ticket de maior risco do projeto até agora**: toca a fonte única de plano do usuário e o gatilho de monetização. Confirmado antes de começar: zero issue/PR existente com "VJT-011" (`search_issues`/`search_pull_requests`, 12 issues e 6 PRs retornados no total do repo, nenhum é VJT-011).
- **O que foi feito**: `useEntitlement()` (`src/hooks/useEntitlement.ts`) como único ponto de leitura de `entitlements` no client — resolve free/premium via `resolveEntitlement` (`src/lib/entitlements.ts`, novo módulo puro), trata `expires_at` vencido como free, assina `postgres_changes` na própria linha (RLS `ent_select_own` já restringe ao próprio usuário) para refletir ativação manual/admin **sem reload da página** enquanto o canal está conectado; se o canal cair (`CHANNEL_ERROR`/`TIMED_OUT`/`CLOSED`), o `.subscribe()` dispara um refetch imediato como backstop (o app desliga `refetchOnWindowFocus` globalmente em `router.tsx`, então esse backstop não é opcional). Migration nova só adiciona `entitlements` à publicação `supabase_realtime`, com guard idempotente (`DO $$ ... EXCEPTION WHEN duplicate_object THEN NULL; END $$;`, mesmo padrão de `20260623022115_...sql`) — **nenhuma policy de escrita adicionada**, confirmado via grep no diff completo (o único `.from("entitlements")` novo é o SELECT de `fetchEntitlement`).
- **Modal de paywall único** (`PaywallModal` + `PaywallProvider`/`usePaywall()`), montado uma única vez em `trip.tsx`, reaproveitado pelos 6 gatilhos via `openPaywall(trigger)` — mesma instância, só o copy muda.
- **Os 6 gatilhos**: (1) criar 2ª viagem — `CreateTripGate` novo em `/trip/novo`, `useOwnedTripCount()`; (2) convidar membro — `InviteMemberCard` novo em `/trip/mais` (stub, fluxo real é VJT-013), `useTripMemberCount()`, free=solo/premium=5; (3) adicionar 6º dia de roteiro — `ItineraryBoard.tsx` troca o mock `CURRENT_PLAN_TIER` por `useEntitlement()`; (4) exportar PDF — mesmo arquivo, `canExportItineraryPdf` (VJT-010) com tier real; (5) esgotar cota de IA — `AiAssistantCard` novo em `/trip/mais` (mock aceito no ticket, chat real é VJT-014), lê `ai_usage` de verdade (10 free/100 premium); (6) abrir item de checklist premium — `PremiumChecklistTeaserRow` novo, linha travada no fim de cada lista quando há templates premium para o tipo (contagem do catálogo completo, não da trip específica — `checklist_items` não guarda linhagem do template de origem, decisão documentada no código).
- `useCreateTrip.ts` para de fazer SELECT ad hoc em `entitlements` — passa a chamar `fetchEntitlement()` (mesma função do hook), preservando leitura sempre fresca no momento da clonagem. Unificação de tipo: `ItineraryPlanTier`/`PlanTier` (antes duplicados em `itinerary.ts`/`trip-templates.ts`) viram aliases de `entitlements.ts`.
- **Arquivos novos**: `src/lib/entitlements.ts`+`.test.ts`, `src/hooks/useEntitlement.ts`+`.test.tsx`, `src/hooks/usePaywall.tsx`+`.test.tsx`, `src/hooks/{useOwnedTripCount,useTripMembers,useAiUsage}.ts`, `src/components/trip/paywall/PaywallModal.tsx`, `src/components/trip/wizard/CreateTripGate.tsx`+`.test.tsx`, `src/components/trip/mais/{PlanStatusCard,InviteMemberCard,AiAssistantCard,MaisDashboard}.tsx`+testes, `src/components/trip/checklists/PremiumChecklistTeaserRow.tsx`+`.test.tsx`, `supabase/migrations/20260725190000_vjt011_entitlements_realtime.sql`. Editados: `trip.tsx` (monta Provider+Modal), `trip.novo.tsx`, `trip.mais.tsx`, `ItineraryBoard.tsx`+teste novo, `useCreateTrip.ts`, `useTripChecklists.ts`, `itinerary.ts`, `trip-templates.ts`, `ChecklistSection.tsx`/`ChecklistsDashboard.tsx`, `trip-checklists.ts`+testes.
- **Tabelas/RLS tocadas**: só a migration de realtime em `entitlements` (sem mudança de policy). Leituras novas em `trips` (contagem por owner), `trip_members` (contagem), `ai_usage` (mês corrente) e `checklist_templates` (contagem premium por tipo) — todas via policies já existentes desde o VJT-001, nenhuma nova.
- **Testes (51 novos, baseline `main` 223 → 274)**: `entitlements.test.ts` 15 (`resolveEntitlement` + as regras puras dos gatilhos 1/2/5) · `useEntitlement.test.tsx` 8 (hook isolado: sem linha, premium ativo, premium expirado, invalidação via `postgres_changes`, e 4 novos casos do backstop de canal — 3 status de falha + 1 confirmando que `SUBSCRIBED` não dispara refetch à toa) · `usePaywall.test.tsx` 9 (modal único: fechado por padrão, copy certo para cada um dos 6 gatilhos via `it.each`, mesma instância reaproveitada, fecha no Close) · `CreateTripGate.test.tsx` 4 (gatilho 1) · `InviteMemberCard.test.tsx` 2 (gatilho 2) · `ItineraryBoard.paywall.test.tsx` 5 (3 do gatilho 3 + 2 do gatilho 4) · `AiAssistantCard.test.tsx` 3 (gatilho 5) · `PremiumChecklistTeaserRow.test.tsx` 2 (gatilho 6) · `trip-checklists.test.ts` 3 novos (`contarTemplatesPremiumPorTipo`, lógica pura do gatilho 6). **QA manual real via Playwright** (viewport 375×812, rede Supabase mockada com `Content-Range`/CORS corretos) nas rotas `/trip/mais`, `/trip/roteiro`, `/trip/checklists`, `/trip/novo` — os 6 paywalls confirmados abrindo com o copy certo na aplicação rodando de verdade (`vite dev`), só o lado free de cada gatilho (o lado premium não bloqueando já está coberto nos testes automatizados acima, não repetido no navegador real); sem erros de console relevantes.
- **Verificação local**: bun 1.3.11 — lint 0 erros (revertidas 4 reformatações incidentais fora de escopo em `src/routes/[.mcp]/*`/`[.well-known]/*`, mesmo padrão do VJT-004b), typecheck 0 erros, test 274/274 (51 novos), build ok.
- **Correções pedidas em revisão humana do diff real do PR #51 (aplicadas nesta mesma sessão, antes do merge)**: (1) a migration não era idempotente (`ALTER PUBLICATION` nu falha com "already member" se reaplicada) — corrigida com o guard `DO $$ ... EXCEPTION WHEN duplicate_object` já usado em outras tabelas do projeto; (2) a alegação "sem reload" no PR/log original estava otimista demais — o hook não tinha nenhum backstop se o canal caísse silenciosamente após conectar (app desliga `refetchOnWindowFocus` globalmente); adicionado backstop via `.subscribe((status) => ...)` disparando refetch em `CHANNEL_ERROR`/`TIMED_OUT`/`CLOSED`, escolhido em vez de `refetchOnWindowFocus` por query por reagir à falha real do canal, não a trocas de foco de janela; (3) a contagem "52 novos" no PR original estava errada — contagem correta e verificada contra a baseline de `main` é 51 (era 47 antes deste backstop, +4 dos novos testes de status do canal). Achados 2 pendências que viraram **VJT-011b** (Seção 7, sem bloqueio): runbook operacional da ativação manual (hoje só documentada em comentário de código) e o teaser premium do checklist contando o catálogo inteiro em vez de filtrar pelas variáveis reais da trip.
- **Como testar no celular**: ver corpo do PR #51 (seção "Como testar no celular").
- **Pendências deixadas para tickets futuros**: checkout Stripe real (VJT-012, CTA do modal fica desabilitado "em breve"); convite real de membro e assistente IA real (VJT-013/VJT-014, ambos com aviso "em breve" quando o gate já libera); runbook de ativação manual + teaser premium filtrado por variáveis da trip (**VJT-011b**, aberto nesta sessão); coluna de linhagem tier em `checklist_items` (fora do escopo do VJT-011b também — mudaria o motor de clonagem do VJT-003, não só a contagem); aplicação manual da migration deste ticket em produção (sem ela, a invalidação em tempo real fica inativa, mas o app continua funcional via leitura normal do React Query, e o backstop de status de canal também não tem o que escutar).
- **Merge**: PR #51 mergeado por `mzinhoww-svg` (`merged_by`), commit de merge `d0f542d`, confirmado via API do GitHub (`merged: true`, `merged_at: 2026-07-25T15:39:36Z`), não assumido. VJT-012 a VJT-017 (todos bloqueados só por VJT-011) e VJT-011b ficam executáveis a partir de agora — a próxima sessão deve reconfirmar ausência de issue/PR duplicado antes de despachar qualquer um deles, conforme Seção 6.

### 2026-07-25 — Revisor automatizado (`.github/workflows/revisor-automatizado.yml`) — **Pausado, baixa prioridade** (PRs [#34](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/34), [#41](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/41), [#42](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/42) mergeados; PR [#43](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/43) fechado sem merge)
- **O que é**: workflow que implementa a subseção "Revisor automatizado" da Seção 6 — dispara só em `pull_request` (`opened`/`ready_for_review`/`synchronize`, com guard `if: draft == false`), lê VIAJALY-TRIP.md, aplica o checklist de 5 pontos contra o diff real, e deveria postar um único comentário no PR.
- **Histórico de correções aplicadas nesta sessão**: (1) PR #34 — workflow inicial; primeira run skipada pela própria validação anti-tamper da `claude-code-action` (esperado, PR introduzia o arquivo). (2) Descoberto e corrigido: secret `ANTHROPIC_API_KEY` ausente no repositório — sem ele a action falhava antes de chamar o modelo. (3) PR #41 — modelo trocado de `claude-opus-5` para `claude-sonnet-5` (custo ~40% menor), gatilho ganhou guard de draft (`ready_for_review` + `if: draft == false`, para não rodar em todo push de correção enquanto o PR ainda está em WIP), `--allowedTools` restritivo removido (hipótese: bloqueava a ferramenta de postar comentário) — run seguinte teve sucesso técnico (sonnet-5, US$1,40, sem erro) mas **nenhum comentário foi postado** (`permission_denials_count: 13`). (4) PR #42 — `show_full_output: true` ligado temporariamente para diagnosticar; run de teste (PR #43, US$0,78, 22 turnos) revelou a causa raiz real: **todas as 12 negações de permissão são chamadas `Bash` de rede (`git fetch`, `gh pr list`, `gh auth status`, `curl`, inclusive uma tentativa do próprio agente com `dangerouslyDisableSandbox: true`, também negada)** — não é, e nunca foi, uma ferramenta de comentário faltando na allowlist; é o sandbox de rede da própria `claude-code-action` bloqueando essas chamadas, independente do conteúdo de `allowedTools`/`disallowedTools`. O agente chegou a montar o comentário correto (checklist aplicado ao diff real, "sem achados") mas não conseguiu publicá-lo.
- **Custo total gasto nas 3 rodadas de diagnóstico**: US$2,29 (opus, PR #38, sem comentário) + US$1,40 (sonnet, PR #38 pós-fix parcial, sem comentário) + US$0,78 (sonnet, PR #43, `show_full_output`, sem comentário) = **US$4,47**, nenhuma delas produziu o entregável.
- **Decisão do humano**: parar por aqui. Verificação manual de PRs (como já vinha sendo feita nesta sessão, sem custo de infraestrutura) continua sendo o caminho até este workflow ser retomado.
- **Estado deixado**: workflow **desativado** (gatilho `pull_request` trocado por `workflow_dispatch` — só dispara manualmente, nunca mais sozinho em PR real) num commit final que também reverte `show_full_output` para `false`, para não continuar gastando API a cada PR sem nunca entregar o comentário. Guard de draft, modelo `claude-sonnet-5` e ausência de `--allowedTools` restritivo ficam preservados no arquivo para quando for retomado. PR #43 (veículo de teste descartável) fechado sem merge. Próxima tentativa (se/quando autorizada) deve investigar o input `additional_permissions` da `claude-code-action` para liberar rede ao `Bash`, ou trocar a estratégia de postagem de comentário para não depender de `gh`/`curl` via Bash — e então reverter o gatilho de volta para `pull_request`.

### 2026-07-25 — VJT-007b Consolidar chave de invalidação do Dashboard em useTripChecklists — **Mergeado** (PR [#47](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/47), issue #45)
- **Contexto de execução em paralelo**: rodou ao lado do VJT-009 (PR #49), conforme a Seção 6, regra 11 — escopo isolado, nenhum dos dois tocou o mesmo arquivo (VJT-007b só em `useTripChecklists.ts`; VJT-009 nunca tocou esse arquivo). Merge confirmado nesta sessão via API do GitHub (`merged: true`, `merged_at: 2026-07-25T14:08:16Z`) e via commit `4296b98` em `origin/main`, não assumido.
- **O que foi feito**: `invalidateChecklists` (`src/hooks/useTripChecklists.ts`) passa a importar `tripDashboardQueryKey` de `src/lib/trip-query-keys.ts` em vez de declarar `["trip","dashboard",tripId]` como literal local — mesma fonte única já usada por `invalidateFinanceiro`/`invalidateSavings` (VJT-006b), fechando a pendência registrada na entrada do VJT-007 acima. `invalidateChecklists` e a chave própria dos checklists (`tripChecklistsQueryKey`, antes uma função não exportada `queryKey`) agora são exportados, seguindo o padrão de `invalidateSavings`/`tripSavingsQueryKey`. Sem mudança de comportamento visível ou de fórmula — o valor invalidado já era idêntico, só a fonte da chave mudou.
- **Arquivos alterados**: `src/hooks/useTripChecklists.ts`. Novo: `src/hooks/trip-checklists-invalidation.test.ts` (teste de regressão cobrindo toggle/add/update/delete de item invalidando checklists+dashboard, e isolamento por `tripId`).
- **Tabelas/RLS**: nenhuma (refactor puro de chave de query, sem migration).
- **Verificação local (relatada no PR)**: bun — lint 0 erros, typecheck 0 erros, test 213/213 (5 novos), build ok.
- **Pendências deixadas para tickets futuros**: nenhuma — fecha o gap que motivou o ticket.

### 2026-07-25 — VJT-009 Item de visto contextual — **Mergeado** (PR [#49](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/49), issue #48)
- **Contexto de execução em paralelo**: rodou ao lado do VJT-007b (PR #47, escopo isolado — nenhum dos dois tocou o mesmo arquivo); por isso a issue/PR não editaram as Seções 0/8, consolidado agora.
- **O que foi feito**: card contextual "Visto" na lista Documentos de `/trip/checklists`, funil para a consultoria, nunca bloqueante (Seção 2). Módulo puro novo `src/lib/trip-visa.ts` (+`.test.ts`, 16 testes): `encontrarPaisVisto` casa `trips.destino_pais` (texto livre) contra `paises_visto.pais_nome` por nome normalizado (sem FK entre as tabelas); `exigeItemVisto` (type guard); `buildVisaConsultoriaLink` usa `paises_visto.link_consultoria` quando cadastrado (preserva query + UTM) ou cai no WhatsApp padrão (`@/lib/whatsapp`/`@/lib/contact`); `trackVisaCtaClick` dispara o evento `visa_cta_click` (`dataLayer`/`gtag`, mesmo padrão de `trackWhatsAppClick`).
- **Hook novo `src/hooks/useTripVistoContextual.ts`**: query key própria `["trip","paises-visto"]`, isolada de `["trip","checklists",tripId]`/`["trip","dashboard",tripId]` — não toca `useTripChecklists.ts` (responsabilidade exclusiva do VJT-007b, rodando em paralelo).
- **Componente novo `VisaContextualCard.tsx`**, plugado via prop opcional `topContent` em `ChecklistSection.tsx`/`ChecklistsDashboard.tsx`, só na lista `documentos`.
- **⚠️ Decisão de produto registrada na Seção 3**: `paises_visto.link_consultoria` está `NULL` nas 14 linhas seedadas desde o VJT-001 (nenhuma migration posterior populou a coluna) — o fallback WhatsApp é hoje o único caminho ativo do CTA, decisão aceita conscientemente pelo dono do produto, não pendência.
- **Arquivos novos**: `src/lib/trip-visa.ts`+`.test.ts`, `src/hooks/useTripVistoContextual.ts`, `src/components/trip/checklists/VisaContextualCard.tsx`. Editados: `ChecklistSection.tsx`, `ChecklistsDashboard.tsx`.
- **Tabelas/RLS**: nenhuma migration nova — só leitura de `paises_visto` via RLS pública `paises_read` já existente desde o VJT-001.
- **Verificação manual real (Playwright)**: `vite dev` + auth falsa via `localStorage` + mock stateful da rede REST do Supabase — trip com destino "Estados Unidos" exibe o card ("Visto para Estados Unidos (B1/B2)"), CTA com `href` correto (`wa.me` + UTM); trip com destino "Portugal" não exibe nada. Sem erros de console em nenhum dos dois casos.
- **Verificação local**: bun 1.3.11 — lint 0 erros, typecheck 0 erros, test 218/218 (16 novos), build ok.
- **Como testar no celular**: `/trip/checklists` com trip para destino que exige visto (EUA/Canadá/Austrália/China/Índia) → card no topo de Documentos; destino sem visto (Portugal/França/Japão) → nenhum card.
- **Pendências deixadas para tickets futuros**: popular `paises_visto.link_consultoria` com URL real via SQL Editor (hoje `NULL`, ver decisão na Seção 3); PostHog real para `visa_cta_click` (VJT-015, bloqueado por VJT-011).

### 2026-07-25 — VJT-007 UI de checklists — **Mergeado fora do fluxo normal** (PR [#39](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/39), issue #35)
- **Nota para constar historicamente**: PR #39 (VJT-007) foi mergeado nesta sessão de troubleshooting do revisor automatizado (a mesma sessão que diagnosticou o secret `ANTHROPIC_API_KEY` ausente e o modelo/gatilho/permissão do workflow `.github/workflows/revisor-automatizado.yml`), não como parte do fluxo normal de review descrito na Seção 6. O merge foi decisão humana, mas sem a etapa usual de revisão dedicada ao diff antes de aprovar — o resumo do PR foi conferido à parte, depois do merge, a pedido do humano (ver achado do VJT-007b acima, encontrado nessa conferência posterior).
- **O que foi feito**: ver corpo do PR #39 e o ticket VJT-007 na Seção 7 para o escopo completo (4 listas de checklist, `trip-checklists.ts` reaproveitando `calcularProgressoChecklists`/`MARCOS_CONHECIDOS`, `useTripChecklists.ts`, componentes em `src/components/trip/checklists/`).
- **Pendência aberta por essa conferência posterior**: VJT-007b (Seção 7) — chave de invalidação do Dashboard duplicada como literal em vez de importar `tripDashboardQueryKey`, e sem teste de regressão para checklists→dashboard.

### 2026-07-24 — VJT-006 Orçamento por categoria + moeda dual — **Mergeado** (PR [#33](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/33), issue #32)
- **Decisão de estrutura**: `/trip/financeiro` virou tela com abas (shadcn `Tabs` já existente, nenhum componente novo) — "Economia Mensal" (VJT-005, movida para dentro da aba sem nenhuma alteração de comportamento/testes) e "Orçamento por Categoria" (este ticket).
- **O que foi feito**: módulo puro novo `src/lib/trip-budget.ts` (+`.test.ts`, 38 testes) — `calcularResumoCategoria`, `calcularTotaisGerais` (delega para `calcularMeta`/`calcularAcumulado` de `trip-math.ts`, sem duplicar agregação), `calcularFaltaPagar`, `montarDadosDonut`, `itemPrecisaCambio`/`algumItemPrecisaCambio`, `validarNovaCategoria`/`validarNovoItem`, `CATEGORIAS_DEFAULT` (9 categorias do PRD), `categoriasDefaultFaltando` (comparação por nome normalizado — trim+lowercase+acentos via NFD, mesmo padrão depois reaproveitado em `trip-visa.ts`/VJT-009). Hook `useTripBudget.ts` + componentes em `src/components/trip/financeiro/budget/`.
- **Correções da 1ª revisão**: cross-invalidation entre as 3 telas do Financeiro (`invalidateFinanceiro` — Orçamento/Economia Mensal/Dashboard); paleta de categorias padrão expandida de 6 para 9; `calcularTotaisGerais` parou de duplicar a agregação do `trip-math.ts`; normalização de acento em `categoriasDefaultFaltando`.
- **Arquivos novos**: `src/lib/trip-budget.ts`+`.test.ts`, `src/hooks/useTripBudget.ts`, `src/components/trip/financeiro/budget/*`. Editado: `src/routes/trip.financeiro.tsx` (shell de abas).
- **Tabelas/RLS**: nenhuma migration nova — usa `budget_categories`/`budget_items`/`trips.moeda_destino`/`trips.cambio_manual` já existentes desde o VJT-001, mesma RLS `bc_all`/`bi_all`/`is_trip_member`.
- **Verificação manual real (Playwright)**: adicionar item com estimado R$1.200 + pago R$400 na aba Orçamento → trocar para a aba Economia Mensal sem reload → barra em 33%, "Meses restantes" e "Sugestão/mês" batendo com `trip-math.ts` para o cenário.
- **Verificação local**: bun 1.3.11 — lint 0 erros, typecheck 0 erros, test 187/187 (38 novos), build ok.
- **Como testar no celular**: `/trip/financeiro` com trip → 2 abas; "Economia Mensal" idêntica ao VJT-005; em "Orçamento por Categoria": sem categorias → CTA "Criar categorias padrão"; adicionar item, definir câmbio, estourar categoria → conferir que a outra aba reflete sem reload.
- **Pendências deixadas para tickets futuros**: paywall/limites por plano (VJT-011); instrumentação PostHog `budget_item_created` (VJT-015).

### 2026-07-24 — VJT-005 Economia mensal — **Mergeado** (PR [#31](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/31), issue #29)
- **O que foi feito**: `/trip/financeiro` (placeholder desde VJT-001) virou dashboard real de Economia Mensal — CRUD de `savings_entries` (adicionar/editar inline/remover, qualquer membro da trip via policy `se_all` já existente), 3 cards de resumo (meses restantes, sugestão mensal, total acumulado), barra combinada (acumulado/meta) e dica dinâmica (`calcularDicaEconomia`), sempre orientando, nunca bloqueando.
- **Módulo novo `src/lib/trip-savings.ts`**: conversões de `mes_ano` (`mesAnoAtual`, `mesAnoFromInputMonth`/`inputMonthFromMesAno`), `formatMesAnoLabel` (pt-BR), `calcularValorRegistradoNoMes` e `calcularDicaEconomia`. Revisado nesta sessão antes do merge: não reimplementa nenhuma fórmula de `trip-math.ts` — a dica só combina outputs já calculados (`modo`, `metaBrlCents`, `acumuladoBrlCents`, `sugestaoMensalBrlCents`) com o valor registrado no mês corrente.
- **Achado desta revisão (vira decisão pendente para VJT-006)**: `/trip/financeiro` foi construída como página de conteúdo único (`SavingsDashboard` empilha tudo verticalmente), **sem nenhuma estrutura de abas**. VJT-006 (Orçamento por categoria) vai estender a mesma rota conforme o próprio PR já anuncia, mas não há um scaffold de abas pronto para receber — vai precisar decidir entre empilhar mais uma seção vertical ou introduzir um componente de abas do zero antes de começar a implementação.
- **Arquivos novos**: `src/lib/trip-savings.ts`+`.test.ts` (24 testes), `src/hooks/useTripSavings.ts`, `src/components/trip/financeiro/{SavingsDashboard,SavingsSummaryCards,SavingsProgressBar,SavingsTip,SavingsEntryForm,SavingsEntryList}.tsx`. Editado: `src/routes/trip.financeiro.tsx`.
- **Tabelas/RLS**: nenhuma migration nova — usa `savings_entries`/`budget_items` já existentes desde VJT-001, policy `for all using (is_trip_member(trip_id))`. Erro `23505` (registro duplicado no mês) capturado e convertido em mensagem amigável.
- **Verificação local (relatada no PR)**: bun 1.3.11 — lint 0 erros, typecheck 0 erros, test 128/128 (24 novos), build ok. CI verde antes do merge.
- **Revisão antes do merge (esta sessão)**: diff completo lido arquivo a arquivo — sem achados bloqueantes, só o ponto de design do VJT-006 acima.
- **Como testar no celular**: `/trip/financeiro` sem trip → CTA "Criar minha viagem". Com trip: dica + barra + 3 cards + formulário + lista; adicionar/editar/remover registro atualiza sem reload; dois registros no mesmo mês → toast de erro amigável; modo sonho/concluída ajustam a dica.
- **Pendências deixadas para tickets futuros**: VJT-006 (orçamento por categoria + moeda dual, mesma rota — decisão de abas pendente, ver achado acima).

### 2026-07-24 — VJT-010 Export PDF do roteiro — **Mergeado** (PR [#30](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/30), issue #28)
- **O que foi feito**: botão "Exportar PDF" em `/trip/roteiro` (só aparece com ≥1 dia no roteiro). `src/lib/itinerary-pdf.ts` (novo, módulo puro + jsPDF) separa montagem de conteúdo testável (`buildItinerarySummary`, `buildDayHeading`, `buildDaySlotLines`, `itineraryPdfFileName`, `buildItineraryPdfDocument`) da geração real (`exportItineraryPdf`), seguindo o mesmo padrão visual de `travel-kit-pdf.ts` (navy/coral, helvetica, A4). Capa com marca, destino, data (ou "Data ainda não definida" em modo sonho), viajantes e total de dias; dias com quebra de página automática.
- **Ponto plugável para paywall**: `canExportItineraryPdf(tier)` em `src/lib/itinerary.ts`, mesmo padrão de `isDayLimitReached`. Revisado nesta sessão antes do merge: é um stub simples e isolado (`tier === "premium"`) — não embute lógica própria de entitlement; a checagem de plano real continua isolada no mock `CURRENT_PLAN_TIER` em `ItineraryBoard.tsx`, que VJT-011 troca por `useEntitlement()` sem tocar em `canExportItineraryPdf`.
- **Arquivos novos**: `src/lib/itinerary-pdf.ts`+`.test.ts` (27 testes). Editados: `src/lib/itinerary.ts` (+`canExportItineraryPdf`, 2 testes novos), `src/hooks/useItinerary.ts` (`useCurrentTrip()` passa a trazer `dataViagem`/`numPessoas`, aditivo), `src/components/trip/roteiro/ItineraryBoard.tsx`.
- **Tabelas/RLS**: nenhuma migration nova; `useCurrentTrip()` seleciona 2 colunas a mais de `trips`, já cobertas pela RLS `is_trip_member` existente.
- **Verificação local (relatada no PR)**: bun 1.3.11 — lint 0 erros, typecheck 0 erros, test 133/133 (29 novos), build ok (chunk `trip.roteiro` com jsPDF). CI verde antes do merge.
- **Revisão antes do merge (esta sessão)**: confirmado no diff que `canExportItineraryPdf` não reimplementa checagem de plano — sem achados.
- **Como testar no celular**: `/trip/roteiro` com dias → botão "Exportar PDF" desabilitado no mock free, com link "Recurso Premium. Conheça" para `/trip/mais`; no mock premium, baixa `roteiro-<slug>.pdf` com capa e todos os dias; modo sonho não quebra; roteiro de 15 dias gera múltiplas páginas.
- **Pendências deixadas para tickets futuros**: paywall real (VJT-011); envio por e-mail/geração server-side (fora de escopo); cálculo automático de `data` por dia (pendência já registrada no log do VJT-008); evento `pdf_exported` (VJT-015, bloqueado por VJT-011).

### 2026-07-24 — VJT-004b Extrair MARCOS_CONHECIDOS para módulo compartilhado — **Mergeado** (PR [#27](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/27), issue #26)
- **O que foi feito**: `MARCOS_CONHECIDOS`/`labelMarco` movidos de `src/lib/trip-journey.ts` para `src/lib/trip-templates.ts` (módulo de domínio dos `checklist_templates`, onde `marco` já é campo modelado); `trip-journey.ts` reexporta ambos, sem alterar nenhum comportamento — mesmos consumidores, mesmo teste existente. Novo teste de identidade de referência (`toBe`) garante que não há segunda lista redeclarada.
- **Arquivos editados**: `src/lib/trip-templates.ts`, `src/lib/trip-journey.ts`, `src/lib/trip-journey.test.ts` (1 teste novo).
- **Tabelas/RLS**: nenhuma (refactor puro, sem migration).
- **Verificação local**: bun 1.3.11 — lint 0 erros (descartadas incidentalmente 4 reformatações não relacionadas em `src/routes/[.mcp]/*`/`[.well-known]/*`, fora do escopo do ticket), typecheck 0 erros, test 114/114 (1 novo), build ok. CI verde antes do merge.
- **Revisão antes do merge (sessão seguinte, ao avaliar #30/#31)**: confirmado que nenhum dos dois PRs paralelos (#30, #31) importa de `trip-journey.ts` — sem conflito, sem achados.
- **Pendências deixadas para tickets futuros**: nenhuma — desbloqueia VJT-007 (UI de checklists), que agora pode consumir `MARCOS_CONHECIDOS`/`labelMarco` de `trip-templates.ts` sem redeclarar.

### 2026-07-24 — VJT-004 Dashboard Sua Jornada — **Mergeado** (PR [#25](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/25), issue #22)
- **O que foi feito**: tela-âncora `/trip` (antes placeholder desde VJT-001), consumindo `trip-math` (VJT-002) sobre os dados clonados pelo wizard (VJT-003). Progresso combinado (`progresso_jornada` + breakdown checklists/financeiro), countdown editável (`data_viagem` inline, inclui voltar a modo sonho), stepper de marcos 90/60/30/15/7 com contagem de itens concluídos por marco, atalhos para Financeiro/Checklists/Roteiro, e os 3 estados corretos (sonho/planejando/concluída com retrospectiva + CTA "Planejar nova viagem").
- **Módulo novo `src/lib/trip-journey.ts`**: `calcularDiasRestantes` (countdown em dias, distinto de `calcularMesesRestantes` do trip-math — granularidade e semântica diferentes) e `construirJourneySteps` (status por marco: concluído/atual/próximo/atrasado/pendente). Revisado nesta sessão antes do merge: não duplica nenhuma fórmula de `trip-math.ts`/`trip-templates.ts`; só reaproveita o tipo `TripMode` e recebe `modo` já calculado via `determinarModoTrip`.
- **Achado desta revisão (virou ticket)**: a constante `MARCOS_CONHECIDOS = [90,60,30,15,7]` é local a `trip-journey.ts` e não existe em nenhum lugar compartilhado — não é duplicação hoje (nada mais declarava essa lista antes), mas VJT-007 (UI de checklists) vai precisar do mesmo conjunto de marcos para agrupar itens por prazo, e duas listas independentes divergem com o tempo. Aberto **VJT-004b** na Seção 7 (bloqueado só por VJT-004, já mergeado) para extrair a constante antes disso acontecer — adicionado como bloqueio extra de VJT-007.
- **Arquivos novos**: `src/lib/trip-journey.ts`+`.test.ts` (16 testes), `src/hooks/useTripDashboard.ts`, `src/components/trip/dashboard/{JourneyDashboard,CountdownCard,JourneyProgressCard,JourneyStepper,ShortcutsGrid,ConcludedSummary,TripDateEditor}.tsx`. Editado: `src/routes/trip.index.tsx` (troca placeholder por `JourneyDashboard` quando há trip).
- **Tabelas/RLS**: nenhuma migration nova; só leitura de `trips`/`budget_items`/`savings_entries`/`checklists`/`checklist_items` e um `update` em `trips.data_viagem`/`status`, já cobertos pelas policies `is_trip_member`/`is_trip_owner` do VJT-001.
- **Verificação local (relatada no PR)**: bun 1.3.11 — lint 0 erros, typecheck 0 erros, test 104/104 (16 novos), build ok. CI verde antes do merge.
- **Revisão antes do merge (esta sessão)**: diff completo lido arquivo a arquivo (não só a descrição do PR) — sem achados bloqueantes.
- **Como testar no celular**: `/trip` sem viagem → empty state igual antes; criar viagem sem data → "Ainda sem data definida", sem countdown; data futura → countdown + stepper destaca o marco certo, editar recalcula sem reload; data passada → retrospectiva + CTA nova viagem; meta financeira zero → "Definir orçamento" no lugar do percentual.
- **Pendências deixadas para tickets futuros**: formulário de NPS no modo concluída (VJT-017, bloqueado por VJT-011); multi-trip real atrás do CTA "Planejar nova viagem" (VJT-011); badge de estouro de orçamento (VJT-006).

### 2026-07-24 — VJT-003b Completar catálogo premium genérico para a faixa 80-120 — **Mergeado** (PR [#24](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/24), issue #21)
- **O que foi feito**: migration aditiva `supabase/migrations/20260724180000_vjt003b_premium_catalog_generic.sql` com 45 novas linhas em `checklist_templates`, todas `tier='premium'` sem condição de `regiao`/`clima`/`destino_pack`/`com_crianca` (11 Documentos, 11 Preparativos, 12 Mala, 11 Compras). Bloco genérico premium passa de 10 para 55 itens. Nenhuma linha existente alterada.
- **Teste de contagem novo**: `src/lib/trip-templates.catalog.test.ts` parseia os `INSERT` reais das 3 migrations seedadas (VJT-001+VJT-003+VJT-003b, na ordem de aplicação) e roda `selecionarTemplates` (motor já existente, não tocado) contra o catálogo de verdade.
- **Contagens conferidas nesta sessão (recalculadas manualmente a partir do diff real, batem com o PR)**: catálogo completo = 142 linhas. Free = 30. Premium sem pack (Tóquio asia/tropical) = 93; (Santiago america_sul/frio) = 94; (NYC america_norte/frio) = 95 — todos dentro de 80-120. **Premium + pack Orlando = 106 (114 com criança); premium + pack Europa = 101 (109 com criança)** — confirma a faixa do PRD (Seção 2) para os destinos com pack.
- **Arquivos novos**: `supabase/migrations/20260724180000_vjt003b_premium_catalog_generic.sql`, `src/lib/trip-templates.catalog.test.ts`.
- **Tabelas/RLS**: `checklist_templates` ganha 45 linhas (mesma RLS de leitura pública `templates_read` desde VJT-001). Nenhuma policy nova.
- **Verificação local (relatada no PR)**: bun 1.3.11 — lint 0 erros, typecheck 0 erros, test 94/94 (5 novos de contagem), build ok. CI verde antes do merge.
- **Revisão antes do merge (esta sessão)**: contas recalculadas manualmente a partir da migration real, batem com o declarado no PR — sem achados.
- **⚠️ Pendência para o humano**: migration **não aplicada no Supabase remoto** — aplicar via SQL Editor (mesma limitação do conector já documentada). Catálogo em produção deveria chegar a 142 linhas após aplicar VJT-001+VJT-003+VJT-003b.
- **Pendências deixadas para tickets futuros**: nenhuma — este ticket fecha o gap que o motivou (VJT-007/VJT-009 agora desbloqueados).

### 2026-07-24 — VJT-003c Validação de data passada no wizard de criação de trip — **Mergeado** (PR [#23](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/23), issue #20)
- **O que foi feito**: `validarNovaTripInput` (`src/lib/trip-wizard.ts`) agora importa e reaproveita `determinarModoTrip` (`trip-math.ts`, VJT-002 — zero linhas tocadas nesse arquivo) para rejeitar data de viagem no passado com mensagem clara, em vez de criar a trip silenciosamente já em modo "concluída". Assinatura ganhou parâmetro `hoje: Date = new Date()` injetável, mesmo padrão do trip-math, para testes determinísticos. Data igual a hoje continua válida; data futura e modo sonho sem mudança de comportamento.
- **Arquivos editados**: `src/lib/trip-wizard.ts`, `src/lib/trip-wizard.test.ts` (3 novos casos: data passada rejeitada, data igual a hoje válida, data futura válida).
- **Tabelas/RLS**: nenhuma (módulo puro, sem migration).
- **Verificação local (relatada no PR)**: bun 1.3.14 — lint 0 erros, typecheck 0 erros, test 91/91 (3 novos), build ok. CI verde antes do merge.
- **Revisão antes do merge (esta sessão)**: confirmado no diff que a validação delega inteiramente para `determinarModoTrip` — não há checagem de data reimplementada à parte. Sem achados.
- **Como testar no celular**: `/trip/novo` → passo 2 → desativar modo sonho → escolher data anterior a hoje → "Avançar" → toast de erro, permanece no passo 2. Hoje ou data futura → avança normalmente.
- **Pendências deixadas para tickets futuros**: nenhuma — ticket pequeno e autocontido.

### 2026-07-24 — VJT-003 Wizard de onboarding + clonagem de templates — **Mergeado** (PR [#19](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/19), issue [#17](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/17)) — resolução da duplicidade com PR #18/issue #16
- **Contexto**: a auditoria anterior (entrada abaixo) encontrou dois PRs completos e independentes para VJT-003 (#18 e #19), nenhum mergeado. Esta sessão comparou os dois diffs de verdade (arquivos completos, não só descrição dos PRs) e decidiu qual manter.
- **Comparação PR #18 (`/trip/onboarding`, issue #16) vs PR #19 (`/trip/novo`, issue #17)**:
  - *Aderência ao PRD (Seção 2: free ~30, premium 80-120 + packs)*: #18 tem catálogo maior no bucket premium genérico (45 itens, chegando a ~93 no total com pack Orlando/Europa — dentro da faixa) mas **não popula `regiao`/`clima`** (decisão deliberada do próprio PR, colunas do schema ficam mortas). #19 usa `regiao`+`clima`+`com_crianca`+`destino_pack` como as issues pediam, mas o bucket genérico é mais raso (10 itens) e o total fica **abaixo de 80** para destinos sem pack correspondente (ex. Ásia, América do Sul) — vira ticket VJT-003b.
  - *Integração com rotas existentes (VJT-001, VJT-008)*: #19 estende o componente compartilhado `TripSectionPlaceholder` (VJT-001) com `ctaTo`/`ctaLabel` e liga o CTA em **`/trip` e `/trip/roteiro`** (VJT-008); reaproveita `determinarModoTrip` do `trip-math.ts` (VJT-002). #18 escreve um empty-state duplicado à mão só em `/trip` (não toca o roteiro) e reimplementa o cálculo de status inline (`dataViagem ? "planejando" : "sonho"`, nunca produz `concluida`) em vez de reusar `trip-math` — viola a regra de fórmulas centralizadas da Seção 2.
  - *Testes*: #19 tem 88/88 (16 em `trip-templates.test.ts` + `trip-wizard.test.ts` dedicado à validação). #18 tem 78/78, só os 16 de `trip-templates`.
  - **Decisão**: manter #19. Nada de exclusivo se perde ao descartar #18 — a filtragem por `min_duracao` já existe e está testada no motor de #19 (`templateAplica`), só falta popular dados reais (mesma lacuna documentada nos dois PRs).
- **Fechamento do PR #18/issue #16 (nesta sessão)**: comentário de justificativa postado em ambos; PR #18 fechado sem merge (`state: closed`); issue #16 fechada com `state_reason: not_planned`. **Branch `claude/vjt-003-onboarding-wizard` NÃO pôde ser apagada**: `git push origin --delete` retornou 403 (política do proxy de git desta sessão bloqueia deleção de ref remota) — fica pendente para exclusão manual (ver Seção 0).
- **O que o PR #19 mergeado entrega**: wizard de 4 passos (`/trip/novo`) com lista curada de 13 destinos + "outro destino" livre; motor de clonagem `src/lib/trip-templates.ts` (`templateAplica`/`selecionarTemplates`/`agruparEmChecklists`); migration aditiva `20260724150000_vjt003_trip_wizard.sql` (`checklist_templates` 15→97 linhas; `trips.num_criancas` nova coluna); hook `src/hooks/useCreateTrip.ts` (orquestra trip+membro+templates+orçamento opcional); CTA "Criar minha viagem" ligado em `/trip` e `/trip/roteiro`.
- **Arquivos novos**: `src/components/trip/wizard/TripWizard.tsx`, `src/hooks/useCreateTrip.ts`, `src/lib/trip-destinations.ts`, `src/lib/trip-templates.ts`+`.test.ts`, `src/lib/trip-wizard.ts`+`.test.ts`, `src/routes/trip.novo.tsx`, `supabase/migrations/20260724150000_vjt003_trip_wizard.sql`. Editados: `src/components/trip/SectionPlaceholder.tsx` (novas props `ctaTo`/`ctaLabel`), `src/components/trip/roteiro/ItineraryBoard.tsx`, `src/routes/trip.index.tsx`, `src/integrations/supabase/types.ts` (patch manual de `num_criancas`), `src/routeTree.gen.ts`.
- **Tabelas/RLS**: `trips` ganha coluna `num_criancas` (aditiva); `checklist_templates` ganha 82 linhas (mesma RLS de leitura pública já existente). Nenhuma policy nova.
- **Verificação local (relatada no PR)**: bun 1.3.14 — lint 0 erros, typecheck 0 erros, test 88/88, build ok. CI verde antes do merge.
- **⚠️ Pendência para o humano (mesmo padrão do VJT-001/VJT-008)**: migration `20260724150000_vjt003_trip_wizard.sql` **não foi aplicada no Supabase remoto** — aplicar via SQL Editor do dashboard (conector Supabase desta conta não acessa `urrlqljlibpzaqnemlwf`). `types.ts` tem só um patch manual de `num_criancas`.
- **Como testar no celular**: `/trip` sem trip → CTA "Criar minha viagem" → 4 passos (destino da lista curada ou "outro destino" livre; data ou modo sonho; viajantes+crianças; orçamento opcional, pode pular) → "Criar viagem" → volta pra `/trip` com o nome da trip. `/trip/roteiro` também oferece o mesmo CTA quando não há trip.
- **Pendências deixadas para tickets futuros**: VJT-003b (catálogo premium abaixo de 80-120 sem pack — já no backlog, próximo da fila, bloqueia VJT-007/VJT-009); VJT-003c (wizard não valida data de viagem no passado — já no backlog, desbloqueado); dashboard "Sua Jornada" (VJT-004) e UI de checklists (VJT-007) ainda consomem placeholder.

### 2026-07-24 — Auditoria de estado (verificação direta, sem suposição)
- **O que foi feito**: sessão de verificação, não de execução de ticket. Checado contra o estado real (GitHub API para PRs/issues; conectores Supabase e Vercel desta conta para dados externos), a pedido do usuário, cobrindo: quais tickets estão de fato mergeados, status de VJT-002/VJT-003, contagens de seed (`paises_visto`/`checklist_templates`) e existência de `VITE_SENTRY_DSN` no Vercel.
- **Tickets mergeados em `main` (confirmado via `merged: true` na API do GitHub, não via suposição/log antigo)**: VJT-001 (PR #11, mergeado 2026-07-24 12:20), VJT-008 (PR #14, mergeado 2026-07-24 13:15), VJT-002 (PR #15, mergeado 2026-07-24 13:49). `git log origin/main` confere com essas três mesclagens.
- **VJT-003 — duplicidade encontrada**: existem **dois PRs abertos (draft) independentes e completos** para o mesmo ticket, nenhum mergeado:
  - PR [#18](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/18) (branch `claude/vjt-003-onboarding-wizard`, closes issue #16, criada 2026-07-24 15:21) — rota `/trip/onboarding`, migration `20260724160000_checklist_templates_full_bank.sql` (15→111 linhas em `checklist_templates`, sem alterar `trips`), módulo `src/lib/trip-templates.ts` + hook `src/hooks/useTripOnboarding.ts`. CI verde (checado via API, commit `a29b15e`).
  - PR [#19](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/19) (branch `claude/vjt-003-wizard-onboarding`, closes issue #17, criada 2026-07-24 15:26) — rota `/trip/novo`, migration `20260724150000_vjt003_trip_wizard.sql` (15→97 linhas em `checklist_templates` + `alter table trips add column num_criancas`), módulo `src/lib/trip-templates.ts` + `src/lib/trip-wizard.ts`/`trip-destinations.ts` + hook `src/hooks/useCreateTrip.ts`. CI verde (checado via API, commit `e5241ae`).
  - As duas sessões rodaram em paralelo sem se ver: issue #16 (15:21) → issue #17 (15:26, 5min depois) → PR #18 (15:32) → PR #19 (15:34, 2min depois). **Nenhum dos dois foi mergeado, fechado ou alterado por esta sessão de auditoria** — escolher qual manter (ou reconciliar/combinar as duas abordagens) é decisão humana. Mergear qualquer um dos dois sem reconciliar o outro provavelmente gera conflito ou trabalho duplicado no schema (`checklist_templates`/`trips`).
- **Contagens de seed (`paises_visto`=14, `checklist_templates`=15) — não verificado**: sem acesso via conector. `mcp__Supabase__list_projects` desta conta só retorna `crm-ai-studio`, `supabase-purple-book` e `the-loyalty` — nenhum é o projeto `urrlqljlibpzaqnemlwf` usado por este app (gerenciado pelo Lovable Cloud, fora do alcance do conector). Sem uma consulta SQL direta, essas contagens continuam **não confirmadas** (nem contra os 15 originais do VJT-001, nem contra os 111/97 propostos pelos PRs duplicados do VJT-003, já que nenhum dos dois foi mergeado).
- **`VITE_SENTRY_DSN` no Vercel — não verificado**: o conector Vercel desta conta só expõe `get_web_analytics`; não há ferramenta para listar/ler variáveis de ambiente do projeto. Precisa checagem manual no painel (Project Settings → Environment Variables).
- **Ação tomada**: apenas atualização da Seção 0 e desta entrada de log, refletindo o estado real encontrado. Nenhum código, PR, merge ou dado do Supabase/Vercel foi alterado nesta sessão.
- **Pendências para a próxima sessão/humano**: (1) decidir entre PR #18/#19 (ou reconciliar) antes de qualquer nova execução que dependa de VJT-003 mergeado (ex.: VJT-004, VJT-007); (2) confirmar as contagens de seed via SQL Editor do dashboard Supabase; (3) confirmar `VITE_SENTRY_DSN` no painel Vercel.

### 2026-07-24 — VJT-002 trip-math + testes — **Mergeado** (PR [#15](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/15), issue [#12](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/12))
- **O que foi feito**: módulo puro `src/lib/trip-math.ts` com todas as fórmulas da Seção 2 (`consolidarValorBRL`, `calcularMeta`, `calcularAcumulado`, `calcularProgressoFinanceiro`, `determinarModoTrip`, `calcularMesesRestantes`, `calcularSugestaoMensal`, `calcularProgressoChecklists`, `calcularProgressoJornada` com pesos como constantes nomeadas exportadas, `categoriaEstourada`, e o orquestrador `calcularTripMath`). Sem I/O, sem acesso a Supabase — recebe dados já carregados.
- **Arquivos novos**: `src/lib/trip-math.ts`, `src/lib/trip-math.test.ts` (44 testes, cobrindo os 5 edge cases obrigatórios: sonho, concluída, meta zero, categoria estourada, divisão por zero).
- **Tabelas/RLS**: nenhuma (módulo puro, OUT de escopo qualquer UI/persistência).
- **Execução em paralelo**: rodou em paralelo com VJT-003 e VJT-008 enquanto VJT-001 ainda aguardava merge; branch recriada a partir do `main` pós-merge de VJT-001 (via cherry-pick do commit de feature) para que o PR final ficasse só com o diff real do ticket (2 arquivos, 472 linhas). Seção 0/8 não foram editadas no PR, por regra — consolidado agora.
- **Como testar**: `bun run test` → 62/62 (44 novos + os já existentes). Sem UI para testar no celular neste ticket (VJT-004 a VJT-007 vão consumir as funções).
- **Verificação local**: lint/typecheck/test/build verdes com bun 1.3.14 (mesma versão do CI).
- **Pendências deixadas para tickets futuros**: nenhuma função ainda é consumida por UI (entra em VJT-004+).

### 2026-07-24 — VJT-008 Roteiro: dias e slots — **Mergeado** (PR [#14](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/14), issue [#13](https://github.com/mzinhoww-svg/viaja-junto-comigo/issues/13))
- **O que foi feito**: módulo puro `src/lib/itinerary.ts` (tipos `SlotPeriod`/`ItineraryDay`/`ItinerarySlot`, geração automática de `dia_numero`/`ordem`, renumeração após remoção, plano de swap para reordenar sem violar `unique(trip_id, dia_numero)`, ponto plugável do limite free `FREE_ITINERARY_DAY_LIMIT = 5`/`getItineraryDayLimit(tier)`); hook `src/hooks/useItinerary.ts` (`useCurrentTrip()` + `useItineraryDays(tripId)` + mutations de adicionar/remover/duplicar/mover dia e editar slot); UI em `src/components/trip/roteiro/` (card de dia colapsável, ações mover/duplicar/remover com confirmação, aviso de limite free); `src/routes/trip.roteiro.tsx` passou do placeholder para a tela real.
- **Arquivos novos**: `src/lib/itinerary.ts`, `src/lib/itinerary.test.ts` (23 testes), `src/hooks/useItinerary.ts`, `src/components/trip/roteiro/*`. Editado: `src/routes/trip.roteiro.tsx`.
- **Tabelas/RLS**: nenhuma migration nova — usa `itinerary_days`/`itinerary_slots` já definidas e com RLS via `is_trip_member` desde a migration do VJT-001.
- **Execução em paralelo**: rodou em paralelo com VJT-002 e VJT-003, bloqueado só por VJT-001; branch partiu de `claude/vjt-001` (só ali existiam schema/rotas/CI reais antes do merge) e foi rebaseada sobre `main` depois que VJT-001 mergeou, reduzindo o diff final ao escopo do ticket. Seção 0/8 não foram editadas no PR, por regra — consolidado agora.
- **Como testar no celular**: `/trip/roteiro` sem trip → empty state; sem dias → CTA "Adicionar Dia 1"; até 15 dias editáveis (onde ir/onde comer/observações), duplicar, remover (renumera), mover com setas, colapsar/expandir; ao atingir 5 dias no plano free, "Adicionar dia" desabilita com aviso.
- **Verificação local**: com bun 1.3.14 (igual ao CI) — lint (0 erros), typecheck (0 erros), test (23/23), build ok.
- **Pendências deixadas para tickets futuros**: export PDF (VJT-010); paywall real (VJT-011 — hoje só desabilita com aviso); cálculo automático de `data` por dia a partir de `data_viagem`.

### 2026-07-24 — VJT-001 Fundação do repositório — **Mergeado** (PR [#11](https://github.com/mzinhoww-svg/viaja-junto-comigo/pull/11))
- **O que foi feito**: schema completo do Trip versionado em migration; rotas `/trip/*` autenticadas (sessão compartilhada com o portal; sem login → redirect `/portal/login`) com bottom nav de 5 itens e placeholders; CI GitHub Actions (lint+typecheck+vitest+build via bun); Sentry no client gated por `VITE_SENTRY_DSN`; vitest com primeira suíte (`money.test.ts`). Commits de suporte: formatação prettier/eslint em 117 arquivos pré-existentes + correção de types Supabase desatualizados (`ab_events`/`ab_results`) — sem isso o CI nasceria vermelho.
- **Arquivos novos**: `supabase/migrations/20260723120000_viajaly_trip_initial_schema.sql`, `src/routes/trip.{tsx,index,financeiro,checklists,roteiro,mais}.tsx`, `src/components/trip/{BottomNav,SectionPlaceholder}.tsx`, `src/lib/sentry.ts`, `src/lib/money.test.ts`, `vitest.config.ts`, `.github/workflows/ci.yml`. Tocados: `package.json` (deps `@sentry/react`, `vitest`; scripts `typecheck`/`test`), `bun.lock`, `src/router.tsx`, `src/routeTree.gen.ts`, `src/integrations/supabase/types.ts`.
- **Tabelas/RLS**: 16 tabelas novas com RLS via `is_trip_member`/`is_trip_owner`, RPC `accept_trip_invite`, seeds `paises_visto` (14) e `checklist_templates` (15).
- **Como testar no celular**: preview Vercel do PR em 375px → `/trip` sem login redireciona; logado, bottom nav navega entre as 5 abas, cada uma com empty state + CTA.
- **Atualização 2026-07-24 (review + migration)**: revisão de código do PR #11 concluída (diff completo conferido — migration idêntica à Seção 4, rotas/gate de auth/bottom nav corretos, sem achados bloqueantes; comentário de revisão postado no PR já que a auto-aprovação do próprio autor não é permitida pelo GitHub). Migration **aplicada manualmente** via SQL Editor do dashboard Supabase (o conector desta conta não acessa `urrlqljlibpzaqnemlwf`, ver nota na Seção 3).
- **Atualização 2026-07-24 (conflito de merge + CI)**: `main` avançou em paralelo com uma feature de MCP/consent-login não relacionada, tocando os mesmos arquivos gerados (`bun.lock`, `src/integrations/supabase/types.ts`, `src/routeTree.gen.ts`) — mergeado `main` na branch, resolvido: `types.ts` passou a usar a versão do `main` (regenerada automaticamente pelo Lovable Cloud após a migration aplicada — validado que já trazia `accept_trip_invite`/`is_trip_member`/`is_trip_owner`, prova indireta de que a migration pegou) mais os tipos de `ab_events`/`ab_results` reinseridos (o gerador do Lovable Cloud não os captura, mesmo gap que o commit original deste PR já tinha corrigido à mão); `routeTree.gen.ts` unificado com as rotas dos dois lados. Primeiro push pós-merge quebrou o CI (`bun.lock` congelado gerado num ambiente com `node_modules` residual de bun 1.3.11, inconsistente com o bun 1.3.14 que o CI resolve como "latest") — corrigido reinstalando do zero com bun 1.3.14. Isso revelou formatação que uma resolução quebrada do prettier vinha mascarando (corrigida via `eslint --fix`) e um bug real pré-existente em `main` (o redirect `?next=` do login do console tornou o parâmetro `next` obrigatório no sistema de tipos, quebrando 3 chamadas de navegação existentes) — só pego agora porque este PR é quem introduz o typecheck no CI; corrigido tornando o tipo genuinamente opcional. CI verde, PR mergeado por decisão humana.
- **Pendências deixadas para tickets futuros**: `VITE_SENTRY_DSN` manual no painel Vercel; confirmar as contagens de seed em produção (`paises_visto` = 14, `checklist_templates` = 15); telas internas (VJT-002+).